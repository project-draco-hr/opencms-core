{
  if (x.length < 2000)   ((PreparedStatement)m_originalStatement).setBytes(parameterIndex,x);
 else   ((PreparedStatement)m_originalStatement).setBinaryStream(parameterIndex,new ByteArrayInputStream(x),x.length);
}
