{
  A_CmsTask task=m_accessTask.createTask(project,currentUser,agent,role,taskType,taskname,timeout,priority);
  if (!taskcomment.equals("")) {
    m_accessTask.writeTaskLog(task.getId(),currentUser,taskcomment);
  }
  return task;
}
