{
  task.setPercentage(percentage);
  task=m_accessTask.writeTask(task);
  m_accessTask.writeTaskLog(task,callingUser,"Percentage was set to " + percentage + "% from "+ callingUser.getFirstname()+ " "+ callingUser.getLastname());
}
