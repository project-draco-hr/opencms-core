{
  A_CmsTask task=readTask(taskId);
  task.setPercentage(1);
  task=m_accessTask.writeTask(task);
  m_accessTask.writeSytemTaskLog(taskId,"Task was accepted from " + currentUser.getFirstname() + " "+ currentUser.getLastname()+ ".");
}
