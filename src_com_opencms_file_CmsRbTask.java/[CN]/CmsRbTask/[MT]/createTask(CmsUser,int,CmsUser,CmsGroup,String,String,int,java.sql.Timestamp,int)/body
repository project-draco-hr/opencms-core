{
  CmsTask task=m_accessTask.createTask(projectid,currentUser,agent,role,tasktype,taskname,timeout,priority);
  if (!taskcomment.equals("")) {
    m_accessTask.writeTaskLog(task.getId(),currentUser,taskcomment);
  }
  return task;
}
