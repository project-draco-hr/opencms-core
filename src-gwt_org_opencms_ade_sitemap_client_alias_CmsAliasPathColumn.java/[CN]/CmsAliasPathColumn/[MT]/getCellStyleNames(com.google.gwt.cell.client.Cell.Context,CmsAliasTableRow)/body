{
  if (object.getAliasError() != null) {
    return super.getCellStyleNames(context,object) + " " + I_CmsCellTableResources.INSTANCE.cellTableStyle().cmsCellError();
  }
 else {
    return super.getCellStyleNames(context,object);
  }
}
