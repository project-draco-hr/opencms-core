{
  if (cms.isAdmin()) {
    return (true);
  }
  if (resource.getOwnerId().equals(currentUser.getId())) {
    if ((resource.getAccessFlags() & flags) == flags) {
      return true;
    }
  }
  return false;
}
