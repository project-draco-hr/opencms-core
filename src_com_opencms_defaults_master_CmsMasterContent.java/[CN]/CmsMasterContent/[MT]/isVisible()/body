{
  CmsUser currentUser=m_cms.getRequestContext().currentUser();
  try {
    if (m_cms.isAdmin()) {
      return true;
    }
 else {
      if (!accessOther(C_ACCESS_PUBLIC_VISIBLE) && !accessOwner(m_cms,currentUser,C_PERMISSION_VIEW) && !accessGroup(m_cms,currentUser,C_ACCESS_GROUP_VISIBLE)) {
        return false;
      }
 else {
        return true;
      }
    }
  }
 catch (  CmsException exc) {
    return false;
  }
}
