{
  CmsUser currentUser=cms.getRequestContext().currentUser();
  if (!(accessOther(C_ACCESS_PUBLIC_WRITE,resource) || accessOwner(cms,currentUser,C_ACCESS_OWNER_WRITE,resource) || accessGroup(cms,currentUser,C_ACCESS_GROUP_WRITE,resource))) {
    return false;
  }
  return true;
}
