{
  String retValue="";
  if (m_dataSet.m_groupId != null) {
    retValue=m_dataSet.m_groupId + "";
  }
  if ((m_dataSet.m_groupName == null || "".equals(m_dataSet.m_groupName.trim())) && (m_dataSet.m_groupId != null && !CmsUUID.getNullUUID().equals(m_dataSet.m_groupId))) {
    try {
      retValue=m_cms.readGroup(m_dataSet.m_groupId).getName();
    }
 catch (    CmsException exc) {
    }
  }
 else {
    retValue=m_dataSet.m_groupName;
  }
  return retValue;
}
