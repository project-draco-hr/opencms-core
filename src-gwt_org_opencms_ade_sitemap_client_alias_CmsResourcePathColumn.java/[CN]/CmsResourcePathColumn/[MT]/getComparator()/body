{
  return new Comparator<CmsAliasTableRow>(){
    public int compare(    CmsAliasTableRow o1,    CmsAliasTableRow o2){
      return o1.getResourcePath().toString().compareTo(o2.getResourcePath().toString());
    }
  }
;
}
