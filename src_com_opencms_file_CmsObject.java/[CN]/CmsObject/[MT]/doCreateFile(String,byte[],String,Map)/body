{
  if (properties == null) {
    properties=new Hashtable();
  }
  CmsFile file=m_driverManager.createFile(m_context.currentUser(),m_context.currentGroup(),m_context.currentProject(),addSiteRoot(newFileName),contents,type,properties);
  return file;
}
