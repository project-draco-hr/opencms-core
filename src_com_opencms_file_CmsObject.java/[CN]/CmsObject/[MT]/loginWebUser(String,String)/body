{
  CmsUser newUser=c_rb.loginWebUser(m_context.currentUser(),m_context.currentProject(),username,password);
  System.err.println("+############");
  System.err.println(newUser);
  init(m_context.getRequest(),m_context.getResponse(),newUser.getName(),newUser.getDefaultGroup().getName(),C_PROJECT_ONLINE_ID);
  System.err.println(this.getRequestContext().currentUser());
  System.err.println("-############");
  return (newUser.getName());
}
