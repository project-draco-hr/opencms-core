{
  CmsFolder cmsFolder=m_driverManager.createFolder(m_context.currentUser(),m_context.currentGroup(),m_context.currentProject(),addSiteRoot(folder + newFolderName + C_FOLDER_SEPARATOR),new Hashtable());
  return cmsFolder;
}
