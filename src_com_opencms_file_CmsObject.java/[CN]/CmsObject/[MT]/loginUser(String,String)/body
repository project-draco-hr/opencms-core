{
  A_CmsUser newUser=readUser(username,password);
  init(m_context.getRequest(),m_context.getResponse(),newUser.getName(),newUser.getDefaultGroup().getName(),C_PROJECT_ONLINE);
  return (newUser);
}
