{
  CmsProject project=m_rb.readProject(m_context.currentUser(),m_context.currentProject(),site.getOnlineProjectId());
  if (project.getParentId() != -1)   return m_rb.getSite(m_context.currentUser(),m_context.currentProject(),project.getParentId());
 else   return null;
}
