{
  CmsPermissionSet permissions=(CmsPermissionSet)m_permissions.get(entry.getPrincipal());
  if (permissions == null)   permissions=new CmsPermissionSet();
  permissions.setPermissions(0,entry.getDeniedPermissions());
}
