{
  if ((d1.getTitle() != null) && (d2.getTitle() != null)) {
    return d1.getTitle().compareTo(d2.getTitle());
  }
  if ((d1.getTitle() == null) && (d2.getTitle() == null)) {
    return 0;
  }
  if (d1.getTitle() == null) {
    return -1;
  }
  return 1;
}
