{
  byte[] newContent=new byte[this.getContents().length];
  System.arraycopy(getContents(),0,newContent,0,getContents().length);
  CmsFile clone=new CmsFile(getId(),getResourceId(),getParentId(),getFileId(),getResourceName(),getType(),getFlags(),getProjectId(),getState(),getLoaderId(),getDateCreated(),getUserCreated(),getDateLastModified(),getUserLastModified(),getLength(),getLinkCount(),newContent);
  return clone;
}
