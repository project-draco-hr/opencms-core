{
  String name=null;
  String extension="";
  int dot;
  name=this.getResourceName();
  dot=name.lastIndexOf(".");
  if (dot > 0) {
    extension=name.substring(dot,name.length());
  }
  return extension;
}
