{
  Map<String,String> result=new HashMap<String,String>();
  if (propertyConfig != null) {
    for (    Map.Entry<String,CmsXmlContentProperty> entry : propertyConfig.entrySet()) {
      CmsXmlContentProperty prop=entry.getValue();
      String defaultValue=getPropValueIds(cms,prop.getType(),prop.getDefault());
      if (defaultValue != null) {
        result.put(entry.getKey(),defaultValue);
      }
    }
  }
  result.putAll(properties);
  return result;
}
