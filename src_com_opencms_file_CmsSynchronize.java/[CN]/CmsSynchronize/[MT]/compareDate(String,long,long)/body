{
  Date syncVfsDate=m_synchronizeList.getVfsDate(resourceName);
  Date syncSfsDate=m_synchronizeList.getSfsDate(resourceName);
  Date fileVfsDate=new Date(vfsDate);
  Date fileSfsDate=new Date(sfsDate);
  if (syncSfsDate == null) {
    return C_SYNC_SFS;
  }
  if (fileVfsDate.after(syncVfsDate)) {
    if (fileSfsDate.after(syncSfsDate)) {
      return C_SYNC_SFSNEW;
    }
 else {
      return C_SYNC_SFS;
    }
  }
 else {
    if (fileSfsDate.after(syncSfsDate)) {
      return C_SYNC_VFS;
    }
 else {
      return C_SYNC_NONE;
    }
  }
}
