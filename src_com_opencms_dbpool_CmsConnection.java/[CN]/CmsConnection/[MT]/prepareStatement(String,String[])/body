{
  checkIsClosed();
  String key=str + ":" + str1;
  if (!m_statementPool.containsKey(key)) {
    m_statementPool.put(key,new CmsPreparedStatement(m_originalConnection.prepareStatement(str,str1),this));
  }
  return (PreparedStatement)m_statementPool.get(key);
}
