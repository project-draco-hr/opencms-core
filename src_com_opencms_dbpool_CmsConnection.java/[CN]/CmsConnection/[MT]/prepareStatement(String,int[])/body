{
  checkIsClosed();
  String key=str + ":" + values;
  if (!m_statementPool.containsKey(key)) {
    m_statementPool.put(key,new CmsPreparedStatement(m_originalConnection.prepareStatement(str,values),this));
  }
  return (PreparedStatement)m_statementPool.get(key);
}
