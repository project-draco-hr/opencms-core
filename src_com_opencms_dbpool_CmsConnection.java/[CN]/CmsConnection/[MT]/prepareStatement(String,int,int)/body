{
  checkIsClosed();
  String key=sql + ":" + a+ ":"+ b;
  if (!m_statementPool.containsKey(key)) {
    m_statementPool.put(key,new CmsPreparedStatement(m_originalConnection.prepareStatement(sql,a,b),this));
  }
  return (PreparedStatement)m_statementPool.get(key);
}
