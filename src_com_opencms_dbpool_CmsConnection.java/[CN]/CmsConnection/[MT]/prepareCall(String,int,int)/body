{
  String key=sql + ":" + a+ ":"+ b;
  if (!m_statementPool.containsKey(key)) {
    m_statementPool.put(key,new CmsCallableStatement(m_originalConnection.prepareCall(sql,a,b),this));
  }
  return (CallableStatement)m_statementPool.get(key);
}
