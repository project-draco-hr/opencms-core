{
  checkIsClosed();
  String key=C_SIMPLE_STATEMENT_KEY;
  if (!m_statementPool.containsKey(key)) {
    m_statementPool.put(key,new CmsStatement(m_originalConnection.createStatement(),this));
  }
  return (Statement)m_statementPool.get(key);
}
