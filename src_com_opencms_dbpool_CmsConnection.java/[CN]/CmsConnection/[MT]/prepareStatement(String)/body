{
  checkIsClosed();
  String key=sql;
  if (!m_statementPool.containsKey(key)) {
    m_statementPool.put(key,new CmsPreparedStatement(m_originalConnection.prepareStatement(sql),this));
  }
  return (PreparedStatement)m_statementPool.get(key);
}
