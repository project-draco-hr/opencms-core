{
  String key=sql;
  if (!m_statementPool.containsKey(key)) {
    m_statementPool.put(key,new CmsCallableStatement(m_originalConnection.prepareCall(sql),this));
  }
  return (CallableStatement)m_statementPool.get(key);
}
