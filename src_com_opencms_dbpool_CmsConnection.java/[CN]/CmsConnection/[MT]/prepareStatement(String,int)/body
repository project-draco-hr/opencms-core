{
  checkIsClosed();
  String key=str + ":" + param;
  if (!m_statementPool.containsKey(key)) {
    m_statementPool.put(key,new CmsPreparedStatement(m_originalConnection.prepareStatement(str,param),this));
  }
  return (PreparedStatement)m_statementPool.get(key);
}
