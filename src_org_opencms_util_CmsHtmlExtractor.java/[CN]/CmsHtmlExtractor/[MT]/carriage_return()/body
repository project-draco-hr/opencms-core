{
  int length;
  length=m_buffer.length();
  if ((0 != length) && ((newline_size <= length) && (!m_buffer.substring(length - newline_size,length).equals(newline)))) {
    m_buffer.append(newline);
  }
}
