{
  String mode=getExplorerMode();
  if (mode == null) {
    mode="explorerview";
  }
  String resource=(String)m_explorerResource.get(mode);
  if (resource == null) {
    resource="/";
  }
  return resource;
}
