{
  ElementState state;
  unindent();
  state=getElementState();
  if (state.empty) {
    printText("/>");
  }
 else {
    if (_format.getIndenting() && !state.preserveSpace && state.afterElement)     breakLine();
    printText("</" + tagName + ">");
  }
  state=leaveElementState();
  if (state != null) {
    state.afterElement=true;
    state.empty=false;
  }
}
