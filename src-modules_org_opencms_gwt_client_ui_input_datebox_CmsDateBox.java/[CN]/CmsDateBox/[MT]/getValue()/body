{
  Date date=null;
  try {
    date=CmsDateConverter.toDate(m_box.getText());
    m_box.setErrorMessage(null);
  }
 catch (  Exception e) {
    m_box.setErrorMessage(Messages.get().key(Messages.ERR_DATEBOX_INVALID_DATE_FORMAT_1,CmsDateConverter.cutSuffix(m_box.getText())));
  }
  return date;
}
