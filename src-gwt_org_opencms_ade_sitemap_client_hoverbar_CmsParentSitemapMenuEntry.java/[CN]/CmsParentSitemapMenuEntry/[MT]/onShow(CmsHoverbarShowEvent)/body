{
  CmsSitemapController controller=getHoverbar().getController();
  boolean show=controller.isRoot(getHoverbar().getEntry().getSitePath()) && (controller.getData().getParentSitemap() != null);
  setVisible(show);
}
