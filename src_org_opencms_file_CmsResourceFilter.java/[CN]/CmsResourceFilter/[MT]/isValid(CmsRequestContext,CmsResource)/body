{
  if (!includeDeleted() && (resource.getState() == I_CmsConstants.C_STATE_DELETED)) {
    return false;
  }
  if (!includeInvalid() && ((resource.getDateReleased() > context.getRequestTime()) || (resource.getDateExpired() < context.getRequestTime()))) {
    return false;
  }
  return true;
}
