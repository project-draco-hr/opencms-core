{
  cleanupExternDependencies(m_elements.deleteElementsAfterPublish());
  for (int i=0; i < invalidTemplates.size(); i++) {
    cleanupExternDependencies(m_elements.deleteElementsByTemplate((String)invalidTemplates.elementAt(i)));
  }
}
