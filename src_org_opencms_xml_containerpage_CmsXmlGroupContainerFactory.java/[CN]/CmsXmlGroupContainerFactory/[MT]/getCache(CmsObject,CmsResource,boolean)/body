{
  if (resource instanceof I_CmsHistoryResource) {
    return null;
  }
  return m_cache.getCacheGroupContainer(m_cache.getCacheKey(resource.getStructureId(),keepEncoding),cms.getRequestContext().getCurrentProject().isOnlineProject());
}
