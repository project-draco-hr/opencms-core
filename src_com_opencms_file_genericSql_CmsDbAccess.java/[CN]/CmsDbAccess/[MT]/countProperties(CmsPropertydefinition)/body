{
  ResultSet result=null;
  PreparedStatement statement=null;
  int returnValue;
  try {
    statement=m_pool.getPreparedStatement(m_cq.C_PROPERTIES_READALL_COUNT_KEY);
    statement.setInt(1,metadef.getId());
    result=statement.executeQuery();
    if (result.next()) {
      returnValue=result.getInt(1);
    }
 else {
      throw new CmsException("[" + this.getClass().getName() + "] "+ metadef.getName(),CmsException.C_UNKNOWN_EXCEPTION);
    }
  }
 catch (  SQLException exc) {
    throw new CmsException("[" + this.getClass().getName() + "] "+ exc.getMessage(),CmsException.C_SQL_ERROR,exc);
  }
 finally {
    if (statement != null) {
      m_pool.putPreparedStatement(m_cq.C_PROPERTIES_READALL_COUNT_KEY,statement);
    }
    if (result != null) {
      try {
        result.close();
      }
 catch (      SQLException se) {
      }
    }
  }
  return returnValue;
}
