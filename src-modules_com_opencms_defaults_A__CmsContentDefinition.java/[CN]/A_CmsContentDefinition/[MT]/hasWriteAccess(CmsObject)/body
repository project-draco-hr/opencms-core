{
  CmsUser currentUser=cms.getRequestContext().currentUser();
  if (isLockable() && (!getLockstate().equals(currentUser.getId()))) {
    return false;
  }
  if (!(accessOther(I_CmsConstants.C_ACCESS_PUBLIC_WRITE) || accessOwner(cms,currentUser,CmsPermissionSet.PERMISSION_WRITE) || accessGroup(cms,currentUser,I_CmsConstants.C_ACCESS_GROUP_WRITE))) {
    return false;
  }
  return true;
}
