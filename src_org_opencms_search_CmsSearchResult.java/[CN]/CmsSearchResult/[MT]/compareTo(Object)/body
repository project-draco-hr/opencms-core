{
  if (object == null || !(object instanceof CmsSearchResult)) {
    return 0;
  }
  try {
    int score=((CmsSearchResult)object).getScore();
    if (m_score > score) {
      return -1;
    }
    if (m_score < score) {
      return 1;
    }
  }
 catch (  Exception e) {
  }
  return 0;
}
