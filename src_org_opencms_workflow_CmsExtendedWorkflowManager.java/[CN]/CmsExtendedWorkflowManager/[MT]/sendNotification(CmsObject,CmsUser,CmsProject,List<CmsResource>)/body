{
  try {
    CmsHtmlMail mail=new CmsHtmlMail();
    String linkHref=OpenCms.getLinkManager().getServerLink(userCms,"/system/modules/org.opencms.ade.publish/publish.jsp?" + CmsPublishService.PARAM_PUBLISH_PROJECT_ID + "="+ workflowProject.getUuid());
    mail.setHtmlMsg(linkHref);
    mail.addTo(recipient.getEmail());
    mail.setSubject("Workflow notification (" + userCms.getRequestContext().getCurrentUser().getName() + ")");
    mail.send();
  }
 catch (  Throwable e) {
    LOG.error(e.getLocalizedMessage(),e);
  }
}
