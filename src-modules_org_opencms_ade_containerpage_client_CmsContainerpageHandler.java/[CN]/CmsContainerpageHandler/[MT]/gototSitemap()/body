{
  if (m_controller.hasPageChanged()) {
    CmsLeavePageDialog dialog=new CmsLeavePageDialog(m_controller.getData().getSitemapUri(),m_controller,this);
    dialog.center();
  }
 else {
    m_controller.leaveUnsaved(m_controller.getData().getSitemapUri());
  }
}
