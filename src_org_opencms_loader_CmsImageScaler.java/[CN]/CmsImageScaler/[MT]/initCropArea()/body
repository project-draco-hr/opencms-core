{
  if (isCropping()) {
    if (m_width < 0) {
      m_width=m_cropWidth;
    }
    if (m_height < 0) {
      m_height=m_cropHeight;
    }
    if ((getType() != 6) && (getType() != 7)) {
      setType(0);
    }
  }
}
