{
  if (isCropping()) {
    if (m_width < 0) {
      m_width=m_cropWidth;
    }
    if (m_height < 0) {
      m_height=m_cropHeight;
    }
    if ((getType() < 1) || (getType() > 2)) {
      setType(0);
    }
  }
}
