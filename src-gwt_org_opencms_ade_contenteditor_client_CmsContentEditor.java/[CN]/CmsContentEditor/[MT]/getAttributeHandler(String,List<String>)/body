{
  List<String> childPathElements=new ArrayList<String>(parentPathElements);
  childPathElements.add(attributeName);
  AttributeHandler handler=getRootAttributeHandler().getHandlerByPath(childPathElements.toArray(new String[childPathElements.size()]));
  return handler;
}
