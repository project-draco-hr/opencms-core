{
  int windowHeight=Window.getClientHeight();
  int bodyHeight=RootPanel.getBodyElement().getOffsetHeight();
  m_basePanel.getElement().getStyle().setProperty("minHeight",windowHeight > bodyHeight ? windowHeight : bodyHeight,Unit.PX);
}
