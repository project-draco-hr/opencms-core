{
  m_editor.saveAndDeleteEntities(m_changedEntityIds,m_deletedEntities,true,new Command(){
    public void execute(){
      setSaved();
      if (m_onClose != null) {
        m_onClose.execute();
      }
      clearEditor();
    }
  }
);
}
