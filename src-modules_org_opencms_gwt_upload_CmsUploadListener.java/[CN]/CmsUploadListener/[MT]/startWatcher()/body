{
  if (m_watcher == null) {
    try {
      m_watcher=new CmsTimeoutWatchDog(this);
      m_watcher.start();
    }
 catch (    Exception e) {
      LOG.info("UUID: " + getId() + " unable to create watchdog: "+ e.getMessage());
    }
  }
}
