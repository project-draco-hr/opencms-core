{
  Date lastSyncDate=null;
  Hashtable dateOfFile=(Hashtable)m_synchronizeList.get(resourceName);
  if (dateOfFile != null) {
    lastSyncDate=(Date)dateOfFile.get(C_SFS_DATE);
  }
  return lastSyncDate;
}
