{
  if ((OpenCms.getRunLevel() > OpenCms.RUNLEVEL_2_INITIALIZING) && m_staticFrozen) {
    throw new CmsConfigurationException(Messages.get().container(Messages.ERR_CONFIG_FROZEN_3,this.getClass().getName(),name,id));
  }
  m_staticFrozen=true;
  super.initConfiguration(name,id,className);
  addTypeId(m_typeId);
}
