{
  CmsFormatterBean selfFormatter=new CmsFormatterBean(CmsFormatterBean.WILDCARD_TYPE,res.getRootPath(),res.getStructureId(),1,Integer.MAX_VALUE,true,false,res.getRootPath());
  return new CmsFormatterConfiguration(Collections.singletonList(selfFormatter));
}
