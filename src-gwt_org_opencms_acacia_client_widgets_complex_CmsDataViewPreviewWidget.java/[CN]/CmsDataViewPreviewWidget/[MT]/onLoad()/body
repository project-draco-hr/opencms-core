{
  super.onLoad();
  CmsCoreProvider.getVfsService().getDataViewThumbnail(m_config,m_accessor.getValue().getId(),new AsyncCallback<String>(){
    public void onFailure(    Throwable caught){
    }
    public void onSuccess(    String imageUrl){
      m_image.setUrl(imageUrl);
    }
  }
);
}
