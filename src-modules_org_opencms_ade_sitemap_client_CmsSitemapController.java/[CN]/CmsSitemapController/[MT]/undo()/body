{
  if (!isDirty()) {
    return;
  }
  if (m_undone.isEmpty()) {
  }
  CmsClientSitemapChange change=m_changes.remove(m_changes.size() - 1);
  m_undone.add(change);
  if (!isDirty()) {
    reset();
  }
}
