{
  try {
    return ((getCms().isAdmin() || getCms().isManagerOfProject()) && (!getCms().getRequestContext().currentProject().isOnlineProject()));
  }
 catch (  CmsException e) {
    return false;
  }
}
