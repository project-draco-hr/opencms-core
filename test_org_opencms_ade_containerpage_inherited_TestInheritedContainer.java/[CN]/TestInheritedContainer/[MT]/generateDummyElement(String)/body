{
  Map<String,String> settings=new HashMap<String,String>();
  settings.put("setting_" + key,"value_" + key);
  CmsContainerElementBean elementBean=new CmsContainerElementBean(CmsUUID.getConstantUUID(key),CmsUUID.getNullUUID(),settings,false);
  return elementBean;
}
