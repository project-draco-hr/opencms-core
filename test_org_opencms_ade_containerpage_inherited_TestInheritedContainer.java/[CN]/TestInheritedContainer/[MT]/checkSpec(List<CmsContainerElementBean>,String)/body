{
  assertTrue("Number of elements does not match the number of specification strings!",specs.length == elements.size());
  for (int i=0; i < elements.size(); i++) {
    CmsContainerElementBean elementBean=elements.get(i);
    String spec=specs[i];
    checkSpec(elementBean,spec);
  }
}
