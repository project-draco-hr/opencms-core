{
  if (accessWrite(currentUser,currentProject,(A_CmsResource)file)) {
    m_fileRb.writeFile(currentProject,onlineProject(currentUser,currentProject),file);
  }
 else {
    throw new CmsException(CmsException.C_EXTXT[CmsException.C_NO_ACCESS],CmsException.C_NO_ACCESS);
  }
}
