{
  CmsGroup role=null;
  if (roleName != null) {
    role=readGroup(currentUser,null,roleName);
  }
  java.sql.Timestamp timestamp=new java.sql.Timestamp(timeout);
  return m_taskRb.createProject(currentUser,projectname,projectType,role,timestamp,priority);
}
