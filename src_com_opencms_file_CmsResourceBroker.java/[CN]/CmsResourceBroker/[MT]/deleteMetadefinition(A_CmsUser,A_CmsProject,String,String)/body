{
  if (isAdmin(currentUser,currentProject)) {
    m_metadefRb.deleteMetadefinition(m_metadefRb.readMetadefinition(name,getResourceType(currentUser,currentProject,resourcetype)));
  }
 else {
    throw new CmsException("[" + this.getClass().getName() + "] "+ name,CmsException.C_NO_ACCESS);
  }
}
