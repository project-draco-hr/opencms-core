{
  if (isAdmin(currentUser,currentProject)) {
    return (m_metadefRb.writeMetadefinition(metadef));
  }
 else {
    throw new CmsException("[" + this.getClass().getName() + "] "+ metadef.getName(),CmsException.C_NO_ACCESS);
  }
}
