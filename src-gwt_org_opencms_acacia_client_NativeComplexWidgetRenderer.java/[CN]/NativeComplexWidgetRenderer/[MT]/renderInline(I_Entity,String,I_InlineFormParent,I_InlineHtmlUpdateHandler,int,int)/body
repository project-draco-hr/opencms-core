{
  I_EntityAttribute attribute=parentEntity.getAttribute(attributeName);
  String renderInline=AcaciaConstants.FUNCTION_RENDER_INLINE;
  if (attribute != null) {
    List<I_Entity> values=attribute.getComplexValues();
    List<Element> elements=Vie.getInstance().getAttributeElements(parentEntity,attributeName,formParent.getElement());
    for (int i=0; i < elements.size(); i++) {
      Element element=elements.get(i);
      if (i < values.size()) {
        I_Entity value=values.get(i);
        renderNative(getNativeInstance(),renderInline,element,value,Vie.getInstance(),m_jsonConfig.getJavaScriptObject());
      }
    }
  }
}
