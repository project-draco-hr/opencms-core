{
  CmsRelationType result=valueOfInternal(name);
  if (result == null) {
    throw new CmsIllegalArgumentException(org.opencms.db.Messages.get().container(org.opencms.db.Messages.ERR_MODE_ENUM_PARSE_2,name,CmsRelationType.class.getName()));
  }
  return result;
}
