{
  this(entry.getId(),entry.getOriginalUri(),entry.getStructureId(),entry.getName(),entry.getTitle(),entry.isRootEntry(),entry.getProperties(),entry.getInheritedProperties(),entry.getContentId());
}
