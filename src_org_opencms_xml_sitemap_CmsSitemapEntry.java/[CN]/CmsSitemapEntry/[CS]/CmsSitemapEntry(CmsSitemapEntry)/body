{
  this(entry.getId(),entry.getOriginalUri(),entry.getStructureId(),entry.getName(),entry.getTitle(),entry.isRootEntry(),entry.getNewProperties(),entry.getComputedProperties(),entry.getContentId(),entry.getContentName());
}
