{
  this(entry.getId(),entry.getOriginalUri(),entry.getResourceId(),entry.getName(),entry.getTitle(),entry.getProperties(),entry.getSubEntries(),entry.getContentId());
  setRuntimeInfo(entry.getInheritedProperties(),entry.getPosition());
}
