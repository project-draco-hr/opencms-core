{
  String translatedTitle=OpenCms.getResourceManager().getFileTranslator().translateResource(value.getStringValue(cms)).replace("/","_");
  return new CmsNumberSuffixNameSequence(translatedTitle);
}
