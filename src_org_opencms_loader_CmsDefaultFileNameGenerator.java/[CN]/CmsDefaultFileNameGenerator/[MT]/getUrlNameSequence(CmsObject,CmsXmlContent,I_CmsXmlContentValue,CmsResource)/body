{
  String translatedTitle=OpenCms.getResourceManager().getFileTranslator().translateResource(value.getStringValue(cms)).replace("/","-");
  return new CmsNumberSuffixNameSequence(translatedTitle);
}
