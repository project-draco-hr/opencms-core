{
  String header=m_cookieBase.formatCookie(new org.apache.commons.httpclient.Cookie(cookie.getDomain(),cookie.getName(),cookie.getValue(),cookie.getPath(),cookie.getMaxAge(),cookie.getSecure()));
  addHeader("Set-Cookie",header);
}
