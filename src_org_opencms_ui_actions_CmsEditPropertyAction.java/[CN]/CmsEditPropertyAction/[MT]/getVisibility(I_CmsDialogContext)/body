{
  if (!(context instanceof I_CmsEditPropertyContext) || !((I_CmsEditPropertyContext)context).isPropertyEditable(m_propertyId)) {
    return CmsMenuItemVisibilityMode.VISIBILITY_INVISIBLE;
  }
 else {
    return getVisibility(context.getCms(),context.getResources());
  }
}
