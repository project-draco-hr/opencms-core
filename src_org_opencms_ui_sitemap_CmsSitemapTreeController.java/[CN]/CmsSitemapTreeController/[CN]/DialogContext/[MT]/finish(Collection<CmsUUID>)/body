{
  closeWindow();
  if (result.isEmpty()) {
    return;
  }
  if (m_node != null) {
    if (m_node == m_currentRootNode) {
      m_localeContext.refreshAll();
    }
 else {
      updateNode(m_node);
    }
  }
}
