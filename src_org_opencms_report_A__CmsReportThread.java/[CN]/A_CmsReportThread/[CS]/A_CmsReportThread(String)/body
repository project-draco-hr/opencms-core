{
  super(OpenCms.getThreadStore().getThreadGroup(),name);
  setDaemon(false);
  m_id=new CmsUUID();
  m_doomed=false;
  setName(name + " [" + m_id+ "]");
  OpenCms.getThreadStore().addThread(this);
}
