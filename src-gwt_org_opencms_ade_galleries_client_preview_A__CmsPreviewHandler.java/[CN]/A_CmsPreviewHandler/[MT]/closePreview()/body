{
  if (m_resourcePreview.getPreviewDialog().getGalleryMode() == GalleryMode.editor) {
    CmsPreviewUtil.enableEditorOk(false);
  }
  m_resourcePreview.getGalleryDialog().getParentPanel().addStyleName(I_CmsLayoutBundle.INSTANCE.previewDialogCss().hidePreview());
}
