{
  if (entry.getProperties().containsKey("sitemap")) {
    return;
  }
  String newColor;
switch (entry.getEditStatus()) {
case edited:
    newColor="#f77";
  break;
case created:
newColor="#8bf";
break;
case normal:
default :
newColor=null;
}
setColor(newColor);
}
