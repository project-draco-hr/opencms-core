{
  super.registerItem(item);
  item.setDragParent(this);
  if (getDnDHandler() != null) {
    if (isDndEnabled()) {
      item.enableDnD(getDnDHandler());
    }
 else {
      item.disableDnD();
    }
  }
}
