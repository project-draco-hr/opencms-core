{
  m_documentTypeConfigs.put(documentType.getName(),documentType);
  if (OpenCms.getLog(CmsLog.CHANNEL_INIT).isInfoEnabled()) {
    OpenCms.getLog(CmsLog.CHANNEL_INIT).info(". Search document types: adding '" + documentType.getName() + "' using handler "+ documentType.getClassName());
  }
}
