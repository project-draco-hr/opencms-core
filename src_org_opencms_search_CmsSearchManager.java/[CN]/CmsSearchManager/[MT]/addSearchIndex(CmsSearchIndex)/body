{
  m_indexes.add(searchIndex);
  if (OpenCms.getLog(CmsLog.CHANNEL_INIT).isInfoEnabled()) {
    OpenCms.getLog(CmsLog.CHANNEL_INIT).info(". Search indexes       : adding '" + searchIndex.getName() + "' for project "+ searchIndex.getProject());
  }
}
