{
  List<CmsContainerElementData> result=null;
  try {
    ensureSession();
    result=getListElementsData(OpenCms.getADEManager().getRecentList(getCmsObject()),containerpageUri,containers,new Locale(locale));
  }
 catch (  Throwable e) {
    error(e);
  }
  return result;
}
