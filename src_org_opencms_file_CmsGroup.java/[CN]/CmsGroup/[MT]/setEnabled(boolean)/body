{
  if (enabled) {
    if (getDisabled()) {
      setFlags(getFlags() ^ I_CmsPrincipal.FLAG_DISABLED);
    }
  }
 else {
    if (!getDisabled()) {
      setFlags(getFlags() ^ I_CmsPrincipal.FLAG_DISABLED);
    }
  }
}
