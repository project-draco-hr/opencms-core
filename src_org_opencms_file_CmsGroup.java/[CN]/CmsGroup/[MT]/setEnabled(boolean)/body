{
  if (enabled) {
    if (getDisabled()) {
      setFlags(getFlags() ^ I_CmsConstants.C_FLAG_DISABLED);
    }
  }
 else {
    if (!getDisabled()) {
      setFlags(getFlags() ^ I_CmsConstants.C_FLAG_DISABLED);
    }
  }
}
