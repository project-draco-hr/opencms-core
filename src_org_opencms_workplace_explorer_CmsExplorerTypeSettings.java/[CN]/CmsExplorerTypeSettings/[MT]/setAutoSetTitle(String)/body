{
  m_autoSetTitle=Boolean.valueOf(autoSetTitle).booleanValue();
  if (OpenCms.getLog(this).isDebugEnabled()) {
    OpenCms.getLog(this).debug(". Auto set title       : " + (m_autoSetTitle ? "true" : "false"));
  }
}
