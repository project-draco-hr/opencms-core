{
  m_autoSetNavigation=Boolean.valueOf(autoSetNavigation).booleanValue();
  if (OpenCms.getLog(this).isDebugEnabled()) {
    OpenCms.getLog(this).debug(". Auto set navigation  : " + (m_autoSetNavigation ? "true" : "false"));
  }
}
