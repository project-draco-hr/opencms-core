{
  CmsGroupDisabledStateAction action=(CmsGroupDisabledStateAction)getMetadata(getListId()).getColumnDefinition(LIST_COLUMN_ICON).getDirectAction(LIST_ACTION_ICON);
  if (action != null) {
    action.setUserName(getParamUsername());
  }
  return super.getList();
}
