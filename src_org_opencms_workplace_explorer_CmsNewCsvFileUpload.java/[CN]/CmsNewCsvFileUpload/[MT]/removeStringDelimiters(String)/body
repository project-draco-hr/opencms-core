{
  String k=key.trim();
  if (k.charAt(0) == C_TEXT_DELIMITER) {
    k=k.substring(1);
  }
  if (k.charAt(k.length() - 1) == C_TEXT_DELIMITER) {
    k=k.substring(0,k.length() - 1);
  }
  return k;
}
