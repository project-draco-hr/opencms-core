{
  try {
    CmsResource res=getCms().readResource(getParamResource(),CmsResourceFilter.ALL);
    if (res.isFolder()) {
      return true;
    }
  }
 catch (  CmsException e) {
  }
  return false;
}
