{
  String link=OpenCms.getLinkManager().substituteLinkForUnknownTarget(CmsLoginUI.m_adminCms,"/system/login",false) + "?" + CmsLoginHelper.PARAM_RESET_PASSWORD;
  A_CmsUI.get().getPage().setLocation(link);
}
