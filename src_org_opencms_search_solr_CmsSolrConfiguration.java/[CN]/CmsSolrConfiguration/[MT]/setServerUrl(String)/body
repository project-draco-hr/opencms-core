{
  try {
    m_serverUrl=new URL(url);
  }
 catch (  MalformedURLException e) {
    throw new CmsConfigurationException(Messages.get().container(Messages.ERR_SOLR_BAD_SERVER_URL_1,m_serverUrl),e);
  }
}
