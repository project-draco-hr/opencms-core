{
  return OpenCms.getResourceManager().matchResourceType(CmsResourceTypeXmlContainerPage.INHERIT_CONTAINER_CONFIG_TYPE_NAME,type) && !CmsResource.isTemporaryFileName(rootPath) && rootPath.endsWith("/" + INHERITANCE_CONFIG_FILE_NAME);
}
