{
  if (m_isContainerBeanMode) {
    return new CmsFieldInfoBean(CmsPropertyDefinition.PROPERTY_TITLE,"",getCmsObject().readPropertyObject(getSitePath(),CmsPropertyDefinition.PROPERTY_TITLE,false).getValue(""));
  }
  return m_formatterInfo.getTitleInfo();
}
