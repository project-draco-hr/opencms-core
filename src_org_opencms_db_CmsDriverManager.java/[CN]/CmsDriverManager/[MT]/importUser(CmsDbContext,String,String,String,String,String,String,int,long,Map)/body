{
  name=name.trim();
  OpenCms.getValidationHandler().checkUserName(CmsOrganizationalUnit.getSimpleName(name));
  CmsUser newUser=m_userDriver.createUser(dbc,new CmsUUID(id),name,password,firstname,lastname,email,0,flags,dateCreated,additionalInfos);
  return newUser;
}
