{
  if (project.getFlags() == I_CmsConstants.C_PROJECT_STATE_UNLOCKED) {
    return m_lockManager.countExclusiveLocksInProject(project);
  }
 else {
    throw new CmsLockException(org.opencms.lock.Messages.get().container(org.opencms.lock.Messages.ERR_RESOURCE_LOCKED_1,project.getName()));
  }
}
