{
  String newName;
  boolean alreadyInUse;
  do {
    newName=nameSeq.next();
    CmsUrlNameMappingFilter filter=CmsUrlNameMappingFilter.ALL.filterName(newName).filterRejectStructureId(structureId);
    List<CmsUrlNameMappingEntry> entriesWithSameName=getVfsDriver(dbc).readUrlNameMappingEntries(dbc,false,filter);
    alreadyInUse=!entriesWithSameName.isEmpty();
  }
 while (alreadyInUse);
  return newName;
}
