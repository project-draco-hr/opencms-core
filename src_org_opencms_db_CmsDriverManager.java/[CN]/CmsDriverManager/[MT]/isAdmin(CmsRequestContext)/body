{
  try {
    return userInGroup(context,CmsRuntimeInfoFactory.getNullRuntimeInfo(),context.currentUser().getName(),OpenCms.getDefaultUsers().getGroupAdministrators());
  }
 catch (  CmsException e) {
    return false;
  }
}
