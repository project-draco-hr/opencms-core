{
  name=name.trim();
  validUsername(name);
  validatePassword(password);
  CmsUser newUser=m_userDriver.createUser(dbc,name,password,description," "," "," ",0,I_CmsConstants.C_FLAG_ENABLED,additionalInfos," ",CmsUser.USER_TYPE_SYSTEMUSER);
  return newUser;
}
