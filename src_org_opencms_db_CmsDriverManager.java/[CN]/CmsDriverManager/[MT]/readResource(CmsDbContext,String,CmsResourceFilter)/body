{
  CmsResource resource=m_vfsDriver.readResource(dbc,dbc.getProjectId().isNullUUID() ? dbc.currentProject().getUuid() : dbc.getProjectId(),resourcePath,filter.includeDeleted());
  updateContextDates(dbc,resource);
  return resource;
}
