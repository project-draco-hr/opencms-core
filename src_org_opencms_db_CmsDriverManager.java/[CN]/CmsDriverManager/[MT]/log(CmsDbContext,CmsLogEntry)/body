{
  if ((dbc == null) || (dbc.getAttribute(CmsLogEntry.ATTR_LOG_ENTRY) != null) || (dbc.getRequestContext().getAttribute(CmsLogEntry.ATTR_LOG_ENTRY) != null)) {
    return;
  }
  dbc.setAttribute(CmsLogEntry.ATTR_LOG_ENTRY,Boolean.TRUE);
  m_projectDriver.log(dbc,logEntry);
}
