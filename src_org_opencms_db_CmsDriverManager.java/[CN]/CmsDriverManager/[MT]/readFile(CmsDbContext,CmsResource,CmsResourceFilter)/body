{
  if (resource.isFolder()) {
    throw new CmsVfsResourceNotFoundException("Trying to access a folder as file " + "(" + resource.getRootPath() + ")");
  }
  CmsFile file=m_vfsDriver.readFile(dbc,dbc.currentProject().getId(),filter.includeDeleted(),resource.getStructureId());
  return file;
}
