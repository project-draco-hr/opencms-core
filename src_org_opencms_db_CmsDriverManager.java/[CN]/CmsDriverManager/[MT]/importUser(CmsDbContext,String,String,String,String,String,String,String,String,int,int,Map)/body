{
  name=name.trim();
  validFilename(name);
  CmsUser newUser=m_userDriver.importUser(dbc,new CmsUUID(id),name,password,description,firstname,lastname,email,0,flags,additionalInfos,address,type,null);
  return newUser;
}
