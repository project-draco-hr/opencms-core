{
  if (resource.isFolder()) {
    return new CmsFolder(resource);
  }
  throw new CmsException("Trying to access a file as a folder " + "(" + resource.getRootPath() + ")",CmsException.C_NOT_FOUND);
}
