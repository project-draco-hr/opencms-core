{
  CmsUser theUser=null;
  try {
    theUser=this.readWebUser(username);
  }
 catch (  CmsException e) {
    if (e.getType() == CmsException.C_NO_USER) {
      theUser=this.readUser(runtimeInfo,username);
    }
 else {
      throw e;
    }
  }
  changeUserType(runtimeInfo,theUser,userType);
}
