{
  return (CmsStringUtil.isNotEmptyOrWhitespaceOnly(request.getParameter(ReqParam.dialogmode.name())) ? request.getParameter(ReqParam.dialogmode.name()) : (String)request.getAttribute(ReqParam.dialogmode.name()));
}
