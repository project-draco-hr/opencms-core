{
  Map<String,CmsXmlSitemap> docs=online ? m_documentsOnline : m_documentsOffline;
  if (docs.containsKey(key)) {
    return;
  }
  docs.put(key,sitemap);
  if (LOG.isDebugEnabled()) {
    LOG.debug(Messages.get().getBundle().key(online ? Messages.LOG_DEBUG_CACHE_SET_ONLINE_2 : Messages.LOG_DEBUG_CACHE_SET_OFFLINE_2,new Object[]{key,sitemap}));
  }
}
