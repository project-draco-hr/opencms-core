{
  if (this.m_mode == 'i' || this.m_mode == 'o' || this.m_mode == 'a') {
    this.push('a');
    this.append("[");
    this.m_comma=false;
    return this;
  }
  throw new JSONException("Misplaced array.");
}
