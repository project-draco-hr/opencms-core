{
  String groupName=getCms().readUser(new CmsUUID(getJsp().getRequest().getParameter(CmsEditGroupDialog.PARAM_GROUPID))).getName();
  getCms().deleteGroup(groupName);
  refreshList();
  actionCloseDialog();
}
