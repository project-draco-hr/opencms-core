{
  String groupName=getCms().readGroup(new CmsUUID(getJsp().getRequest().getParameter(CmsEditGroupDialog.PARAM_GROUPID))).getName();
  getCms().deleteGroup(groupName);
  refreshList();
  actionCloseDialog();
}
