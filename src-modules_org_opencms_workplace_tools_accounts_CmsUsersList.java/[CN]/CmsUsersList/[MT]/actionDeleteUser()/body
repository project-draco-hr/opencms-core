{
  String userName=getCms().readUser(new CmsUUID(getJsp().getRequest().getParameter(CmsEditUserDialog.PARAM_USERID))).getName();
  getCms().deleteUser(userName);
  refreshList();
  actionCloseDialog();
}
