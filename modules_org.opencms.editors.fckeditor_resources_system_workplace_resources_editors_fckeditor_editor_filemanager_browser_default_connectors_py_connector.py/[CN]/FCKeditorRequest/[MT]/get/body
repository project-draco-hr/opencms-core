def get(self, key, default=None):
    value = None
    if self.isZope():
        value = self.request.get(key, default)
    elif (key in self.request.keys()):
        value = self.request[key].value
    else:
        value = default
    return value
