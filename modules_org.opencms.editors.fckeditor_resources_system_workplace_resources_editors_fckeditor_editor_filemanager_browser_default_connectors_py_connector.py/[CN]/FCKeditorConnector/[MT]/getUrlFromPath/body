def getUrlFromPath(self, resourceType, path):
    if ((resourceType is None) or (resourceType == '')):
        url = ('%s%s' % (self.removeFromEnd(self.getUserFilesFolder(), '/'), path))
    else:
        url = ('%s%s%s' % (self.getUserFilesFolder(), resourceType, path))
    return url
