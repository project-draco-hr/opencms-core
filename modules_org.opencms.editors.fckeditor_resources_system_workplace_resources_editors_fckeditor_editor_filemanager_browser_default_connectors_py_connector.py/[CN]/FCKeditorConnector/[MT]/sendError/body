def sendError(self, number, text):
    self.setXmlHeaders()
    s = ''
    s += '<?xml version="1.0" encoding="utf-8" ?>'
    s += '<Connector>'
    s += ('<Error number="%s" text="%s" />' % (number, text))
    s += '</Connector>'
    return s
