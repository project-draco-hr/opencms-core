def serverMapFolder(self, resourceType, folderPath):
    resourceTypeFolder = ('%s%s/' % (self.getUserFilesFolder(), resourceType))
    self.createServerFolder(resourceTypeFolder)
    return ('%s%s' % (resourceTypeFolder, self.removeFromStart(folderPath, '/')))
