def getWebUrlFromPath(self, resourceType, path):
    if ((resourceType is None) or (resourceType == '')):
        url = ('%s%s' % (self.removeFromEnd(self.getWebUserFilesFolder(), '/'), path))
    else:
        url = ('%s%s%s' % (self.getWebUserFilesFolder(), resourceType, path))
    return url
