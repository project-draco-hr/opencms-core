{
  try {
    List<CmsResource> folderContent=cms.getResourcesInFolder(CmsResource.getParentFolder(cms.getRequestContext().removeSiteRoot(getRootPath())),CmsResourceFilter.DEFAULT);
    readDependencies(cms,folderContent);
  }
 catch (  CmsException e) {
    LOG.error("Unable to read dependencies for " + getRootPath(),e);
  }
}
