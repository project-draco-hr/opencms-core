{
  if (CmsStringUtil.isEmptyOrWhitespaceOnly(state)) {
    List<NavEntry> subNav=new ArrayList<NavEntry>();
    subNav.add(new NavEntry("Add Project","Adds a new project.",new ExternalResource(OpenCmsTheme.getImageLink(ICON_ADD)),PATH_NAME_ADD));
    subNav.add(new NavEntry("Project History","Displays the project history.",new ExternalResource(OpenCmsTheme.getImageLink(ICON_HISTORY)),PATH_NAME_HISTORY));
    return subNav;
  }
  return null;
}
