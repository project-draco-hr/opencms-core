{
  String[] fields=new String[]{prop.getPropertyName(),prop.getPropertyType(),prop.getWidget(),prop.getWidgetConfiguration(),prop.getRuleRegex(),prop.getRuleType(),prop.getDefault(),prop.getNiceName(),prop.getDescription(),prop.getError()};
  for (  String field : fields) {
    streamWriter.writeString(field);
  }
}
