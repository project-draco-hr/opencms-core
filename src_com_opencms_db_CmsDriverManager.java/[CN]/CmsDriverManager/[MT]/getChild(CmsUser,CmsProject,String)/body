{
  if (!anonymousUser(currentUser,currentProject).equals(currentUser)) {
    return m_projectDriver.getChild(groupname);
  }
 else {
    throw new CmsException("[" + getClass().getName() + "] "+ groupname,CmsException.C_NO_ACCESS);
  }
}
