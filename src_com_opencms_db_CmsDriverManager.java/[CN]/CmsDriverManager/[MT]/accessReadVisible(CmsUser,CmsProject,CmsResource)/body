{
  if ((resource == null) || !accessProject(currentUser,currentProject,resource.getProjectId()) || (checkPermissions(currentUser,resource,C_PERMISSION_READ))) {
    return false;
  }
  return true;
}
