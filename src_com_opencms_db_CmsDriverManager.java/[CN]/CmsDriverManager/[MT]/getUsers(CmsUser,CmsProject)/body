{
  if (!anonymousUser(currentUser,currentProject).equals(currentUser)) {
    return m_userDriver.getUsers(C_USER_TYPE_SYSTEMUSER);
  }
 else {
    throw new CmsException("[" + getClass().getName() + "] "+ currentUser.getName(),CmsException.C_NO_ACCESS);
  }
}
