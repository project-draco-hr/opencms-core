{
  if (isAdmin(currentUser,currentProject)) {
    m_userDriver.writeGroup(group);
    m_groupCache.put(new CacheId(group),group);
  }
 else {
    throw new CmsException("[" + getClass().getName() + "] "+ group.getName(),CmsException.C_NO_ACCESS);
  }
}
