{
  if (m_driverManager.userInGroup(m_user,m_currentProject,m_user.getName(),groupname)) {
    m_currentGroup=m_driverManager.readGroup(m_user,groupname);
  }
 else {
    throw new CmsException("[" + this.getClass().getName() + "] "+ groupname,CmsException.C_NO_ACCESS);
  }
}
