{
  if (m_driverManager.userInGroup(this,m_user.getName(),groupname)) {
    m_currentGroup=m_driverManager.readGroup(this,groupname);
  }
 else {
    throw new CmsException("[" + this.getClass().getName() + "] "+ groupname,CmsException.C_NO_ACCESS);
  }
}
