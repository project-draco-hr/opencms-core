{
  if (resource instanceof I_CmsHistoryResource) {
    return null;
  }
  return m_cache.getCacheSubContainer(m_cache.getCacheKey(resource.getStructureId(),keepEncoding),cms.getRequestContext().currentProject().isOnlineProject());
}
