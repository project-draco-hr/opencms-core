{
  if (xmlSubContainer.getFile() instanceof I_CmsHistoryResource) {
    return;
  }
  boolean online=cms.getRequestContext().currentProject().isOnlineProject();
  m_cache.setCacheSubContainer(m_cache.getCacheKey(xmlSubContainer.getFile().getStructureId(),keepEncoding),xmlSubContainer,online);
}
