{
  Object o=m_doc.getFieldValue(fieldName);
  if (o != null) {
    try {
      return DateField.parseDate(o.toString());
    }
 catch (    ParseException e) {
      LOG.debug(e);
    }
  }
  return null;
}
