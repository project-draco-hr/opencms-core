{
  String ret="";
  int num;
  for (int i=0; i < value.length(); i++) {
    num=value.charAt(i);
    if (num > 127) {
      ret+="&#" + num + ";";
    }
 else {
      ret+=value.charAt(i);
    }
  }
  return ret + "";
}
