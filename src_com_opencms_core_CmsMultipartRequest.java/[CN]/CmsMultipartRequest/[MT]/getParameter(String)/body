{
  try {
    String param=m_req.getParameter(name);
    if (param == null) {
      param=(String)m_parameters.get(name);
      if (param.equals(""))       return null;
    }
    return param;
  }
 catch (  Exception e) {
    return null;
  }
}
