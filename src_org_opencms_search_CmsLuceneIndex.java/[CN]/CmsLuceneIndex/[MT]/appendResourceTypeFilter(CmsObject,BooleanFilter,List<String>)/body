{
  if ((resourceTypes != null) && (resourceTypes.size() > 0)) {
    filter.add(new FilterClause(getMultiTermQueryFilter(I_CmsSearchField.FIELD_TYPE,resourceTypes),BooleanClause.Occur.MUST));
  }
  return filter;
}
