{
  if (!detectAndroid()) {
    return false;
  }
  if (detectOperaMobile()) {
    return false;
  }
  if (userAgent.indexOf(deviceHtcFlyer) != -1) {
    return false;
  }
  if ((userAgent.indexOf(mobile) > -1)) {
    return false;
  }
 else {
    return true;
  }
}
