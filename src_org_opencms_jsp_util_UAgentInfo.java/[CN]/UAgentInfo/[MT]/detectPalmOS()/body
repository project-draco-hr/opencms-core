{
  if ((userAgent.indexOf(devicePalm) != -1) || (userAgent.indexOf(engineBlazer) != -1) || (userAgent.indexOf(engineXiino) != -1)) {
    if (detectPalmWebOS()) {
      return false;
    }
 else {
      return true;
    }
  }
  return false;
}
