{
  Tokenizer toke=new Tokenizer(str);
  str=toke.nextToken();
  if ("null".equals(str)) {
    m_historyId=null;
  }
 else {
    m_historyId=str;
  }
  str=toke.nextToken();
  m_publishTag=Integer.parseInt(str);
  str=toke.nextToken();
  if ("null".equals(str)) {
    m_resourcePath=null;
  }
 else {
    m_resourcePath=str;
  }
  str=toke.nextToken();
  if ("null".equals(str)) {
    m_structureId=null;
  }
 else {
    m_structureId=str;
  }
}
