{
  Tokenizer toke=new Tokenizer(str);
  str=toke.nextToken();
  if ("null".equals(str)) {
    m_dataKey=null;
  }
 else {
    m_dataKey=str;
  }
  str=toke.nextToken();
  if ("null".equals(str)) {
    m_userId=null;
  }
 else {
    m_userId=str;
  }
}
