{
  Tokenizer toke=new Tokenizer(str);
  str=toke.nextToken();
  if ("null".equals(str)) {
    m_userId=null;
  }
 else {
    m_userId=str;
  }
  str=toke.nextToken();
  m_visitDate=Long.parseLong(str);
}
