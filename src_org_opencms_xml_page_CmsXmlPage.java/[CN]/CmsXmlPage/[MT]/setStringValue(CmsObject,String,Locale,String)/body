{
  CmsXmlHtmlValue value=(CmsXmlHtmlValue)getValue(name,locale);
  if (value != null) {
    value.setStringValue(cms,content);
  }
 else {
    throw new CmsXmlException(Messages.get().container(Messages.ERR_XML_PAGE_INVALID_ELEM_SELECT_2,locale,name));
  }
}
