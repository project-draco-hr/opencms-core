{
  List<CmsContainerElementData> result=null;
  try {
    ensureSession();
    CmsResource containerpage=getCmsObject().readResource(pageStructureId);
    String containerpageUri=getCmsObject().getSitePath(containerpage);
    result=getListElementsData(OpenCms.getADEConfigurationManager().getRecentList(getCmsObject()),containerpageUri,containers,new Locale(locale));
  }
 catch (  Throwable e) {
    error(e);
  }
  return result;
}
