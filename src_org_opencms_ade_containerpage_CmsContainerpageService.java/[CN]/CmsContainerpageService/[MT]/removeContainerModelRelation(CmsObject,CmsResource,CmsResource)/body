{
  ensureLock(containerModel);
  cms.deleteRelationsFromResource(containerModel,CmsRelationFilter.relationsToStructureId(containerPage.getStructureId()).filterType(CmsRelationType.CONTAINER_MODEL));
  tryUnlock(containerModel);
}
