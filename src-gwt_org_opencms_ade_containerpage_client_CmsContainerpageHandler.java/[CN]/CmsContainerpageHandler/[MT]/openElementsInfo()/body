{
  CmsResourceInfoDialog.load(CmsCoreProvider.get().getStructureId(),true,CmsContainerpageController.get().getData().getDetailId(),new CloseHandler<PopupPanel>(){
    public void onClose(    CloseEvent<PopupPanel> event){
      deactivateCurrentButton();
      activateSelection();
    }
  }
);
}
