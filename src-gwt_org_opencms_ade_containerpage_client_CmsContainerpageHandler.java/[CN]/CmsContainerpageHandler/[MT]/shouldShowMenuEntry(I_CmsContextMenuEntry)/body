{
  if (m_controller.isEditingDisabled()) {
    String name=entry.getName();
    if ((name != null) && (name.equals(CmsAvailabilityDialog.class.getName()) || name.equals(CmsEditProperties.class.getName()))) {
      return false;
    }
  }
  return true;
}
