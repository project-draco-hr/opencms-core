{
  if (response == null) {
    return null;
  }
  Pattern pattern=Pattern.compile("<!--" + key + "=(.*?)-->");
  Matcher matcher=pattern.matcher(response);
  if (matcher.find()) {
    return matcher.group(1);
  }
 else {
    return null;
  }
}
