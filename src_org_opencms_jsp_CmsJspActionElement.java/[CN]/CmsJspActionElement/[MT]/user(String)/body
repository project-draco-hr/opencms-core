{
  if (isNotInitialized()) {
    return C_NOT_INITIALIZED;
  }
  try {
    return CmsJspTagUser.userTagAction(property,getRequest());
  }
 catch (  Throwable t) {
    handleException(t);
  }
  CmsMessageContainer msgContainer=new CmsMessageContainer(Messages.get(),Messages.LOG_ERR_USER_PROP_READ_1,new Object[]{property});
  return this.getMessage(msgContainer);
}
