{
  try {
    return CmsJspTagInfo.infoTagAction(property,getRequest());
  }
 catch (  Throwable t) {
    handleException(t);
  }
  CmsMessageContainer msgContainer=new CmsMessageContainer(Messages.get(),Messages.LOG_ERR_INFO_PROP_READ_1,new Object[]{property});
  return this.getMessage(msgContainer);
}
