{
  for (  String param : params) {
    String value=request.getParameter(param);
    if (value == null) {
      if (result != null) {
        storeErrorMissingParam(result,param);
      }
      return false;
    }
  }
  return true;
}
