{
  getOkButton().setEnabled(false);
  getCancelButton().setEnabled(false);
  if (getHandler() != null) {
    if (event.getSource().equals(getOkButton())) {
      getHandler().onOk();
    }
 else {
      getHandler().onCancel();
    }
  }
  hide();
}
