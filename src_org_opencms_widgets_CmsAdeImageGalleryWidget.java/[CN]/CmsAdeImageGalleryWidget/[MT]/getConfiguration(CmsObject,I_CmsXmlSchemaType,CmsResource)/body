{
  CmsMessages messages=new CmsMessages(resource.getName(),"en");
  CmsVfsImageWidgetConfiguration config=new CmsVfsImageWidgetConfiguration(cms,messages,null,getConfiguration());
  String result=config.getConfigString();
  return result;
}
