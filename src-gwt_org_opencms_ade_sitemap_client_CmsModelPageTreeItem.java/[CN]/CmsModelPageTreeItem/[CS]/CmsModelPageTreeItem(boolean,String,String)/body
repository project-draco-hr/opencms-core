{
  super(true);
  m_isModelGroup=isModelGroup;
  CmsListInfoBean infoBean=new CmsListInfoBean(title,subTitle,null);
  CmsListItemWidget content=new CmsListItemWidget(infoBean);
  content.setIcon(CmsIconUtil.getResourceIconClasses(isModelGroup ? CmsGwtConstants.TYPE_MODELGROUP_PAGE : CmsGwtConstants.TYPE_MODELPAGE,false));
  initContent(content);
}
