{
  super(true);
  CmsListInfoBean infoBean=new CmsListInfoBean(Messages.get().key(Messages.GUI_MODEL_PAGE_TREE_ROOT_TITLE_0),"",null);
  CmsListItemWidget content=new CmsListItemWidget(infoBean);
  content.setIcon(CmsIconUtil.getResourceIconClasses("containerpage",false));
  initContent(content);
}
