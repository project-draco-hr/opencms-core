{
  String selector="[about='" + entityId + "'][property='"+ attributeName+ "'], [about='"+ entityId+ "'] [property='"+ attributeName+ "']";
  if (context == null) {
    context=Document.get().getDocumentElement();
  }
  return select(selector,context);
}
