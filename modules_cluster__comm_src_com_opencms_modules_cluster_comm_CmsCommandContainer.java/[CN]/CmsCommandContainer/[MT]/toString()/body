{
  StringBuffer ret=new StringBuffer();
  ret.append(getUsername() + ",").append("***,").append(getWaitForResult() + ",{");
  ArrayList list=getCommands();
  for (int i=0; i < list.size(); i++) {
    ret.append(list.get(i).toString());
  }
  ret.append("}");
  return ret.toString();
}
