{
  try {
    m_externLinkValidator.validateLinks(m_cms);
  }
 catch (  Exception e) {
    getReport().println(e);
    if (LOG.isErrorEnabled()) {
      LOG.error(e.getLocalizedMessage());
    }
  }
}
