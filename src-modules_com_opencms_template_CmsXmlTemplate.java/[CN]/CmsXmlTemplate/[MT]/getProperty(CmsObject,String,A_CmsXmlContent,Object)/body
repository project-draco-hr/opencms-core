{
  String requestedUri=cms.getRequestContext().getUri();
  String value="";
  try {
    value=cms.readProperty(requestedUri,tagcontent);
  }
 catch (  Exception e) {
    if (CmsLog.getLog(this).isWarnEnabled()) {
      CmsLog.getLog(this).warn("Usermethod getProperty throwed an Exception getting " + tagcontent,e);
    }
  }
  if (value == null) {
    value="";
  }
  return value;
}
