{
  boolean result=(m_currentEditLevel == -1) || (m_currentEditLevel == container.getContainerLevel());
  CmsDebugLog.consoleLog("container " + container.getContainerId() + " matches level "+ m_currentEditLevel+ " "+ result);
  return result;
}
