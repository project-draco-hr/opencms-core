{
  return element.hasViewPermission() && (!element.hasModelGroupParent() || getData().isModelGroup()) && (m_elementView.equals(element.getElementView()) || isGroupcontainerEditing())&& isContainerEditable(dragParent);
}
