{
  return element.hasViewPermission() && (!element.hasModelGroupParent() || getData().isModelGroup()) && (matchRootView(element.getElementView()) || isGroupcontainerEditing())&& isContainerEditable(dragParent);
}
