{
  return !getData().isUseClassicEditor() && CmsStringUtil.isEmptyOrWhitespaceOnly(element.getNoEditReason()) && hasActiveSelection()&& m_elementView.equals(element.getElementView())&& isContainerEditable(dragParent)&& (!(dragParent instanceof CmsGroupContainerElementPanel) || isGroupcontainerEditing());
}
