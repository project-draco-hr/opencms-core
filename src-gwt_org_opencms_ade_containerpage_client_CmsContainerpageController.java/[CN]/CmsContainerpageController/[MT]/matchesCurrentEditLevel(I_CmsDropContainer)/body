{
  boolean result=!(container instanceof CmsContainerPageContainer) || (m_currentEditLevel == -1) || (m_currentEditLevel == ((CmsContainerPageContainer)container).getContainerLevel());
  return result;
}
