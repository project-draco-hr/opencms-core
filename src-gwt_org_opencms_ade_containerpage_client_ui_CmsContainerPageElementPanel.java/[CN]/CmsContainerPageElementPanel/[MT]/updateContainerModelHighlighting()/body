{
  if (m_containerModel && (m_containerModelHighlighting != null) && RootPanel.getBodyElement().isOrHasChild(getElement())) {
    m_containerModelHighlighting.setPosition(CmsPositionBean.getBoundingClientRect(getElement()));
  }
}
