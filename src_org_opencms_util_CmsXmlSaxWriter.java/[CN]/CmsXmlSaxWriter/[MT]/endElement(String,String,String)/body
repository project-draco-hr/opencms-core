{
  String elementName=resolveName(localName,qualifiedName);
  if (m_openTag) {
    write("/>");
  }
 else {
    if (!elementName.equals(m_lastElementName)) {
      writeNewLine();
    }
    write("</");
    write(elementName);
    write(">");
  }
  m_openTag=false;
  m_indentLevel--;
}
