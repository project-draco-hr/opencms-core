{
  try {
    if (m_openTag) {
      write("/>");
      m_openTag=false;
    }
    writeNewLine();
    m_writer.flush();
  }
 catch (  IOException e) {
    throw new SAXException(Messages.get().key(Messages.ERR_IOERROR_0),e);
  }
}
