{
  if (resource.isFolder()) {
    m_targetFolder.setValue(resource);
  }
 else {
    throw new CmsIllegalArgumentException(Messages.get().container(org.opencms.workplace.commons.Messages.ERR_COPY_MULTI_TARGET_NOFOLDER_1,A_CmsUI.getCmsObject().getSitePath(resource)));
  }
}
