{
  CmsResource resource=readResource(resourcename,CmsResourceFilter.IGNORE_EXPIRATION);
  getResourceType(resource.getTypeId()).setDateLastModified(this,m_securityManager,resource,dateLastModified,recursive);
}
