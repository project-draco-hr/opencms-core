{
  CmsResource resource=readResource(resourcename,CmsResourceFilter.IGNORE_EXPIRATION);
  getResourceType(resource.getTypeId()).setDateReleased(this,m_securityManager,resource,dateReleased,recursive);
}
