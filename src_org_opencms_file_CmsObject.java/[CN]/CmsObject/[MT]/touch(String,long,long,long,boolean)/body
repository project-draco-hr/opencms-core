{
  CmsResource resource=readResource(resourcename,CmsResourceFilter.IGNORE_EXPIRATION);
  getResourceType(resource.getTypeId()).touch(this,m_driverManager,resource,dateLastModified,dateReleased,dateExpired,recursive);
}
