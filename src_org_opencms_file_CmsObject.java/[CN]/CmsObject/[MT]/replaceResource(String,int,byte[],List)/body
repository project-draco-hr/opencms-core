{
  CmsResource resource=readResource(resourcename,CmsResourceFilter.IGNORE_EXPIRATION);
  getResourceType(resource.getTypeId()).replaceResource(this,m_driverManager,resource,type,content,properties);
}
