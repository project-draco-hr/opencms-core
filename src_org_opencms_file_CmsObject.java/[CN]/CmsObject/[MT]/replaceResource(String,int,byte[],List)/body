{
  CmsResource resource=readResource(resourcename,CmsResourceFilter.IGNORE_EXPIRATION);
  getResourceType(resource.getTypeId()).replaceResource(this,m_securityManager,resource,type,content,properties);
}
