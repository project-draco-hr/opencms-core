{
  StringBuffer sb=new StringBuffer();
  String prefetchedData=exportDictionary(CmsPostUploadDialogBean.DICT_NAME,I_CmsPostUploadDialogService.class.getMethod("prefetch"),getDialogData());
  sb.append(prefetchedData);
  sb.append(ClientMessages.get().export(getRequest()));
  return sb.toString();
}
