{
  StringBuffer buf=new StringBuffer(64);
  buf.append(C_UNKNOWN_KEY_EXTENSION);
  buf.append(" ");
  buf.append(keyName);
  buf.append(" ");
  buf.append(C_UNKNOWN_KEY_EXTENSION);
  return buf.toString();
}
