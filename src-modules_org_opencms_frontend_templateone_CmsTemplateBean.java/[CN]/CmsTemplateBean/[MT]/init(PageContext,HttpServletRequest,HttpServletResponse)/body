{
  super.init(context,req,res);
  String className=getClass().getName();
  if (className.endsWith("Bean")) {
    m_properties=properties("search");
    initPageVersion();
    m_showHeadNavigation=!showPrintVersion() && Boolean.valueOf(property(PROPERTY_SHOW_HEADNAV,"search",CmsStringUtil.TRUE)).booleanValue();
  }
}
