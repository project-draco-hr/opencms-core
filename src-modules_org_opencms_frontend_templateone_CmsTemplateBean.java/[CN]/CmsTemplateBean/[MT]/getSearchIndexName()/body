{
  String currentSite=getRequestContext().getSiteRoot();
  if (currentSite.indexOf("sites/default") == -1) {
    return currentSite;
  }
 else {
    return CmsProject.ONLINE_PROJECT_NAME + " project " + getRequestContext().getLocale().toString().toUpperCase()+ " (VFS)";
  }
}
