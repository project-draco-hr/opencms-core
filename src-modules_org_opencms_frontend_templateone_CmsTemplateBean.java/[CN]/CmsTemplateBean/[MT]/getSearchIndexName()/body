{
  String currentSite=getRequestContext().getSiteRoot();
  if (currentSite.indexOf("sites/default") == -1) {
    return currentSite;
  }
 else {
    return getRequestContext().currentProject().getName() + " project " + getRequestContext().getLocale().toString().toUpperCase()+ " (VFS)";
  }
}
