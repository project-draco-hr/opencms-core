{
  String name;
  name=tag.getTagName();
  if (name.equalsIgnoreCase("PRE")) {
    m_isPre=true;
  }
 else   if (name.equalsIgnoreCase("SCRIPT")) {
    m_isScript=true;
  }
  if (tag.breaksFlow()) {
    carriage_return();
  }
}
