{
  if (move) {
    return recursive ? CmsResourceUndoMode.MODE_UNDO_MOVE_CONTENT_RECURSIVE : CmsResourceUndoMode.MODE_UNDO_MOVE_CONTENT;
  }
 else {
    return recursive ? CmsResourceUndoMode.MODE_UNDO_CONTENT_RECURSIVE : CmsResourceUndoMode.MODE_UNDO_CONTENT;
  }
}
