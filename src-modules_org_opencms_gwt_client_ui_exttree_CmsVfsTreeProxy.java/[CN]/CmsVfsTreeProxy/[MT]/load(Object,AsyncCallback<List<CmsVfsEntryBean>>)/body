{
  final I_CmsVfsServiceAsync svc=GWT.create(I_CmsVfsService.class);
  if (loadConfig == null) {
    svc.getRootEntries(callback);
  }
 else {
    BeanModel beanModel=(BeanModel)loadConfig;
    CmsVfsEntryBean entry=beanModel.getBean();
    svc.getChildren(entry.getPath(),callback);
  }
}
