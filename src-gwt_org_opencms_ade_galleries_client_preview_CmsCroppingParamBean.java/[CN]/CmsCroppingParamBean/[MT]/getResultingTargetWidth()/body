{
  int width=-1;
  if ((m_targetHeight > -1) || (m_targetWidth > -1)) {
    if (m_targetWidth > -1) {
      width=m_targetWidth;
    }
 else {
      width=(int)Math.floor(((1.00 * m_orgWidth) / m_orgHeight) * m_targetHeight);
    }
  }
  return width;
}
