{
  StringReader reader=new StringReader(xmlData);
  try {
    return unmarshal(cms,new InputSource(reader));
  }
  finally {
    try {
      reader.close();
    }
 catch (    Exception exc) {
    }
  }
}
