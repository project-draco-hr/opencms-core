{
  if (key == null) {
    return null;
  }
  CmsElementVariant result=(CmsElementVariant)m_variants.get(key);
  if (result != null && result.size() == 0) {
    result=null;
  }
  if (CmsLog.getLog(this).isInfoEnabled()) {
    if (result != null) {
      CmsLog.getLog(this).info(toString() + " getting variant \"" + key+ "\" from cache. ");
    }
 else {
      CmsLog.getLog(this).info(toString() + " Variant \"" + key+ "\" is not in element cache. ");
    }
  }
  return result;
}
