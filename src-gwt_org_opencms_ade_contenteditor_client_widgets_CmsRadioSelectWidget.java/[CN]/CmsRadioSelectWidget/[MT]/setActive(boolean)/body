{
  if (m_active == active) {
    return;
  }
  m_active=active;
  for (int i=0; i < m_arrayRadioButtons.length; i++) {
    m_arrayRadioButtons[i].setEnabled(active);
  }
  if (active) {
    fireChangeEvent();
  }
}
