{
  m_active=active;
  Iterator<CmsRadioButton> i=m_listRadioButtons.iterator();
  while (i.hasNext()) {
    CmsRadioButton rb=i.next();
    rb.setEnabled(active);
  }
  if (active) {
    fireChangeEvent();
  }
}
