{
  if (x > 255) {
    x=255;
  }
  if (x < 0) {
    x=0;
  }
  if (y > 255) {
    y=255;
  }
  if (y < 0) {
    y=0;
  }
  x-=7;
  y-=7;
  m_slider.getElement().getStyle().setLeft(x,Unit.PX);
  m_slider.getElement().getStyle().setTop(y,Unit.PX);
}
