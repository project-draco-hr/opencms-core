{
  name=hyphenize(name);
  String propVal=getComputedStyle(elem,name);
  if ("opacity".equals(name) && ((propVal == null) || (propVal.trim().length() == 0))) {
    propVal="1";
  }
  return propVal;
}
