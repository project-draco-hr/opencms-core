{
  if (getParamListAction().equals(LIST_ACTION_REFRESH)) {
    refreshList();
  }
 else {
    throw new RuntimeException(Messages.get().key(Messages.ERR_LIST_UNSUPPORTED_ACTION_2,getList().getName(),getParamListAction()));
  }
}
