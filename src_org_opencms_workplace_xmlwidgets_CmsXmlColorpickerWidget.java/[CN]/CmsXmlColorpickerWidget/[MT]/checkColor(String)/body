{
  if (color != null) {
    if (color.indexOf("#") == -1) {
      color="#" + color;
    }
    int colLength=color.length();
    if (colLength == 4 || colLength == 7) {
      return color;
    }
  }
  return "#FFFFFF";
}
