{
  try {
    A_CmsUser user=m_cms.addUser(name,password,group,description,new Hashtable(),C_FLAG_ENABLED);
    user.setEmail(email);
    user.setFirstname(firstname);
    user.setLastname(lastname);
    user.setAdditionalInfo("test","AS");
    m_cms.writeUser(user);
    A_CmsUser newUser=m_cms.readUser(name);
    System.out.println(user);
    System.out.println(user.getFirstname());
    System.out.println(user.getLastname());
    System.out.println(user.getEmail());
    System.out.println(user.getAdditionalInfo("test"));
  }
 catch (  Exception exc) {
    printException(exc);
  }
}
