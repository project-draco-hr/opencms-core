{
  try {
    Vector metadefs=m_cms.readAllMetadefinitions(resourcetype,Integer.parseInt(type));
    for (int i=0; i < metadefs.size(); i++) {
      System.out.println((A_CmsMetadefinition)metadefs.elementAt(i));
    }
  }
 catch (  Exception exc) {
    printException(exc);
  }
}
