{
  CmsShell shell=new CmsShell();
  try {
    if ((args.length == 0) || (args.length > 3)) {
      shell.usage();
    }
 else {
      shell.init(args);
      shell.version();
      shell.commands();
    }
  }
 catch (  Exception exc) {
    System.out.println(exc);
  }
}
