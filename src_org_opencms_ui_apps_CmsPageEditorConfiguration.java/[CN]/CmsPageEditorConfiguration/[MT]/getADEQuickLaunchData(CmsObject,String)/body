{
  if (!getVisibility(cms).isActive()) {
    return Optional.absent();
  }
 else {
    return Optional.of(new CmsQuickLaunchData(Q_PAGEEDITOR,null,null,getName(OpenCms.getWorkplaceManager().getWorkplaceLocale(cms)),getImageLink(),false));
  }
}
