{
  if (m_log.isDebugEnabled()) {
    m_log.debug("Visiting " + value.getPath());
  }
  m_errorHandler=value.getContentDefinition().getContentHandler().resolveValidation(m_cms,value,m_errorHandler);
}
