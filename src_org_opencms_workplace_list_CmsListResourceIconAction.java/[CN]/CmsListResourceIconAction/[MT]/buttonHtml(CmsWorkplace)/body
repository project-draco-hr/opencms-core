{
  if (!isVisible()) {
    return "";
  }
  return defButtonHtml(wp.getJsp(),getId() + getItem().getId(),getId(),resolveName(wp.getLocale()),resolveHelpText(wp.getLocale()),isEnabled(),getIconPath(),resolveOnClic(wp.getLocale()),getColumnForTexts() == null);
}
