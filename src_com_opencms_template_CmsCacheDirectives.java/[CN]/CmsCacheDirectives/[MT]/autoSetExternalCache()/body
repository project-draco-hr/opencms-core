{
  if (m_changeCd) {
    boolean proxPriv=m_uri && (m_cacheParameter == null || m_cacheParameter.isEmpty()) && isInternalCacheable();
    boolean proxPubl=proxPriv && m_user;
    boolean export=proxPubl;
    setExternalCaching(isInternalCacheable(),proxPriv,proxPubl,export,isStreamable());
  }
}
