{
  createDigest();
  getImportResource();
  m_docXml=CmsXmlUtils.unmarshalHelper(getFileBytes(CmsImportExportManager.EXPORT_XMLFILENAME),null);
  try {
    m_importVersion=Integer.parseInt(((Element)m_docXml.selectNodes("//" + CmsImportExportManager.N_VERSION).get(0)).getTextTrim());
  }
 catch (  Exception e) {
  }
}
