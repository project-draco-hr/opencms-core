{
  openImportFile();
  try {
    if (m_cms.isAdmin()) {
      importGroups();
      importUsers();
    }
    importAllResources(null,null,null,null,null);
  }
 catch (  CmsException e) {
    throw e;
  }
 finally {
    closeImportFile();
  }
}
