{
  CmsUser currentUser=cms.getRequestContext().currentUser();
  if (!accessOther(C_ACCESS_PUBLIC_READ) && !accessOwner(cms,currentUser,C_PERMISSION_READ) && !accessGroup(cms,currentUser,C_ACCESS_GROUP_READ)) {
    return false;
  }
  return true;
}
