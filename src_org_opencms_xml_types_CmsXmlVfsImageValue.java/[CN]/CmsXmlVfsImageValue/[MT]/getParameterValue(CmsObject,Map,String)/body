{
  String result=null;
  String[] params=((String[])parameterMap.get(key));
  if (params != null && params.length > 0) {
    result=params[0];
  }
  if (result == null) {
    return "";
  }
  return result;
}
