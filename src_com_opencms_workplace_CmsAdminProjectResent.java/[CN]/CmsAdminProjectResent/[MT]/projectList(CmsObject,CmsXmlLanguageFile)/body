{
  Vector list=new Vector();
  Vector mp=cms.getAllManageableProjects();
  Hashtable temp=new Hashtable();
  for (int i=0; i < mp.size(); i++)   temp.put("" + ((CmsProject)mp.elementAt(i)).getId(),mp.elementAt(i));
  Vector ap=cms.getAllAccessibleProjects();
  for (int i=0; i < ap.size(); i++)   if (temp.containsKey("" + ((CmsProject)ap.elementAt(i)).getId()))   list.addElement(ap.elementAt(i));
  return list;
}
