{
  CmsDragContainerElement dragElement=new CmsDragContainerElement(element,dragParent,clientId,sitePath,noEditReason,hasProps);
  enableDragHandler(dragElement);
  addOptionBar(dragElement);
  return dragElement;
}
