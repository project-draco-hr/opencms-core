{
  if ((m_current != m_startInfo) || (Math.abs(m_currentTarget.getWidgetIndex((Widget)m_current.getDraggable()) - m_currentTarget.getWidgetIndex(m_current.getPlaceholder())) > 1)) {
    CmsContainerpageDataProvider.get().setPageChanged();
  }
  m_currentTarget.insert((Widget)m_current.getDraggable(),m_currentTarget.getWidgetIndex(m_current.getPlaceholder()));
}
