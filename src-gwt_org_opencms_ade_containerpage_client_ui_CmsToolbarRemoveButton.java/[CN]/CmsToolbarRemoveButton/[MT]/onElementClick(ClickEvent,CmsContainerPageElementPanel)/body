{
  CmsDomUtil.ensureMouseOut(element.getElementOptionBar().getElement());
  getHandler().removeElement(element,CmsContainerpageController.isConfirmRemove() ? ElementRemoveMode.confirmRemove : ElementRemoveMode.saveAndCheckReferences);
}
