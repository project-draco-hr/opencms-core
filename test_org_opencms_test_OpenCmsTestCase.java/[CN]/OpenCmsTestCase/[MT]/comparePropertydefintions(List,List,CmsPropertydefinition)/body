{
  String noMatches="";
  Iterator i=source.iterator();
  while (i.hasNext()) {
    CmsPropertydefinition prop=(CmsPropertydefinition)i.next();
    if ((!target.contains(prop)) && (!prop.getName().equals(exclude.getName())) && (prop.getType() != exclude.getType())) {
      noMatches+="[" + prop + "]";
    }
  }
  return noMatches;
}
