{
  CmsSetupDb setupDb;
  setupDb=dropTables();
  checkErrors(setupDb);
  setupDb.closeConnection();
  setupDb=dropDatabase();
  if (!"oracle".equals(m_dbProduct)) {
    checkErrors(setupDb);
  }
  setupDb.closeConnection();
}
