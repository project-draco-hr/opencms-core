{
  try {
    return convertToByte(htmlInput);
  }
 catch (  Exception e) {
    if (OpenCms.getLog(this).isWarnEnabled()) {
      OpenCms.getLog(this).warn("[" + this.getClass().getName() + "] "+ " convertToByteSilent, error converting HTML, ",e);
    }
    return htmlInput;
  }
}
