{
  List elements=new ArrayList();
  int count=0;
  Object o;
  do {
    String path=createXpathElement(name,count);
    o=getBookmark(path,locale);
    if (o != null) {
      elements.add(o);
      count++;
    }
  }
 while (o != null);
  return elements;
}
