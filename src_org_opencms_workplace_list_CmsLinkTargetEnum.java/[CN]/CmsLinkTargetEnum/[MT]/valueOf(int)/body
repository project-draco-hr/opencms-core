{
  Iterator iter=VALUES.iterator();
  while (iter.hasNext()) {
    CmsLinkTargetEnum target=(CmsLinkTargetEnum)iter.next();
    if (value == target.getType()) {
      return target;
    }
  }
  throw new IllegalArgumentException(Messages.get().key(Messages.ERR_LIST_ENUM_PARSE_2,new Integer(value),CmsLinkTargetEnum.class.getName()));
}
