{
  CmsShell shell;
  try {
    if (args.length < 1) {
      CmsShell.usage();
    }
 else {
      shell=new CmsShell(args);
      shell.commands();
    }
  }
 catch (  Exception exc) {
    exc.printStackTrace();
  }
}
