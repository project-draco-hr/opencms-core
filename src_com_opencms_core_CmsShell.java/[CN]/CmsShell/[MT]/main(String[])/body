{
  CmsShell shell;
  try {
    if (args.length < 2) {
      CmsShell.usage();
    }
 else {
      shell=new CmsShell(args);
      shell.commands();
    }
  }
 catch (  Exception exc) {
    exc.printStackTrace();
  }
}
