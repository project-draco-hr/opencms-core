{
  CmsShell shell=new CmsShell();
  try {
    if ((args.length == 0) || (args.length > 1)) {
      shell.usage();
    }
 else {
      shell.init(args);
      shell.version();
      shell.copyright();
      shell.commands();
    }
  }
 catch (  Exception exc) {
    exc.printStackTrace();
  }
}
