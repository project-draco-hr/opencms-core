{
  if (m_type == null) {
    return true;
  }
switch (type) {
case ERROR:
    return !m_type.equals(Type.ERROR);
case NORMAL:
  return false;
case WARNING:
return m_type.equals(Type.NORMAL);
default :
}
return true;
}
