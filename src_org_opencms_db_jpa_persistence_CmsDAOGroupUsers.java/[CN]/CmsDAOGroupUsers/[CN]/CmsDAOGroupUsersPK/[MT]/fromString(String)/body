{
  Tokenizer toke=new Tokenizer(str);
  str=toke.nextToken();
  if ("null".equals(str)) {
    m_groupId=null;
  }
 else {
    m_groupId=str;
  }
  str=toke.nextToken();
  if ("null".equals(str)) {
    m_userId=null;
  }
 else {
    m_userId=str;
  }
}
