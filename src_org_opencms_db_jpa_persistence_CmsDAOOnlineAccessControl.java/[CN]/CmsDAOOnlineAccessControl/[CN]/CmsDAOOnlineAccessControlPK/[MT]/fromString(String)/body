{
  Tokenizer toke=new Tokenizer(str);
  str=toke.nextToken();
  if ("null".equals(str)) {
    m_principalId=null;
  }
 else {
    m_principalId=str;
  }
  str=toke.nextToken();
  if ("null".equals(str)) {
    m_resourceId=null;
  }
 else {
    m_resourceId=str;
  }
}
