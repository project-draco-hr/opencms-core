{
  CmsSite site=OpenCms.getSiteManager().getSiteForSiteRoot(m_siteRoot);
  if ((site != null) && (site.getSiteMatcher() != null)) {
    return new CmsSite(getSiteRoot(),(CmsUUID)site.getSiteRootUUID().clone(),getTitle(),new CmsSiteMatcher(m_server),String.valueOf(m_position),CmsStringUtil.isNotEmptyOrWhitespaceOnly(m_secureUrl) ? new CmsSiteMatcher(m_secureUrl) : null,m_exclusiveUrl,m_exclusiveError,m_aliases);
  }
  return null;
}
