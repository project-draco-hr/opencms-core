{
  if (value == null) {
    value="";
  }
  if (value instanceof String) {
    String strValue=(String)value;
    m_colorValue=(strValue);
    if (strValue.length() > 0) {
      checkvalue();
    }
  }
}
