{
  Vector removedElement=m_elements.put(desc,element);
  if (removedElement != null && m_dependenciesExtern != null) {
    removeElementFromDependencies((CmsElementDescriptor)removedElement.firstElement(),(A_CmsElement)removedElement.lastElement());
  }
}
