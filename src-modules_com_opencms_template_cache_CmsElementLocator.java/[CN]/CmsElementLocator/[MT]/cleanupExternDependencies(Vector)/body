{
  if (elements != null) {
    for (int i=0; i < elements.size(); i++) {
      Vector actElement=(Vector)elements.elementAt(i);
      removeElementFromDependencies((CmsElementDescriptor)actElement.firstElement(),(A_CmsElement)actElement.lastElement());
    }
  }
}
