{
  return getHoverbar().getController().isEditable() && !CmsSitemapView.getInstance().isModelPageMode() && (!CmsSitemapView.getInstance().isGalleryMode() || getHoverbar().getController().getData().isGalleryManager());
}
