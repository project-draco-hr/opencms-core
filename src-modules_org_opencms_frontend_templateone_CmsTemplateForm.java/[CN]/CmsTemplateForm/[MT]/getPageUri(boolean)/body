{
  if (removeParams) {
    String uri=m_pageUri;
    if (uri != null && uri.indexOf("?") != -1) {
      uri=uri.substring(0,uri.indexOf("?"));
    }
    return uri;
  }
 else {
    return m_pageUri;
  }
}
