{
  if (w == m_resize) {
    try {
      orphan(m_resize);
    }
  finally {
      getElement().removeChild(m_resize.getElement());
    }
    return true;
  }
 else {
    return super.remove(w);
  }
}
