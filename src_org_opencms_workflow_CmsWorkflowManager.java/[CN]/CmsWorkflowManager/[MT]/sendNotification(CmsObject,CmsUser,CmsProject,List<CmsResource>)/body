{
  try {
    CmsWorkflowNotification notification=new CmsWorkflowNotification(m_adminCms,recipient,userCms.getRequestContext().getCurrentUser(),workflowProject,resources);
    notification.send();
  }
 catch (  EmailException e) {
    LOG.error(e.getLocalizedMessage(),e);
  }
}
