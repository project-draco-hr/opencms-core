{
  try {
    CmsWorkflowNotification notification=new CmsWorkflowNotification(userCms.getRequestContext().getCurrentUser(),workflowProject,resources);
    notification.send();
  }
 catch (  EmailException e) {
    LOG.error(e.getLocalizedMessage(),e);
  }
}
