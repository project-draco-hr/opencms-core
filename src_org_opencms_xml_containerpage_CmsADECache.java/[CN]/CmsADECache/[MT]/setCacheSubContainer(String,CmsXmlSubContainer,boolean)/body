{
  if (online) {
    m_subContainersOnline.put(key,subContainer);
    if (LOG.isDebugEnabled()) {
      LOG.debug(Messages.get().getBundle().key(Messages.LOG_DEBUG_CACHE_SET_ONLINE_2,new Object[]{key,subContainer}));
    }
  }
 else {
    m_subContainersOffline.put(key,subContainer);
    if (LOG.isDebugEnabled()) {
      LOG.debug(Messages.get().getBundle().key(Messages.LOG_DEBUG_CACHE_SET_OFFLINE_2,new Object[]{key,subContainer}));
    }
  }
}
