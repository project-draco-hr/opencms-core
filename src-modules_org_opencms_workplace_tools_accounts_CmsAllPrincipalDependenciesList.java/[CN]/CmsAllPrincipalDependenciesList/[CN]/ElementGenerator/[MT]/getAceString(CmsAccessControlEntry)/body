{
  String result=ace.getPermissions().getPermissionString() + ace.getResponsibleString() + ace.getInheritingString();
  if ((ace.getFlags() & CmsAccessControlEntry.ACCESS_FLAGS_OVERWRITE) != 0) {
    result=result + " (overwrite)";
  }
  return result;
}
