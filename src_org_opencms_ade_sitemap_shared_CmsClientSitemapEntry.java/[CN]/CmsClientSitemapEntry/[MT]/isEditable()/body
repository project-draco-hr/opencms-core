{
  return CmsStringUtil.isEmptyOrWhitespaceOnly(m_noEditReason) && !hasForeignFolderLock() && !hasBlockingLockedChildren()&& (((getLock() == null) || (getLock().getLockOwner() == null)) || getLock().isOwnedByUser());
}
