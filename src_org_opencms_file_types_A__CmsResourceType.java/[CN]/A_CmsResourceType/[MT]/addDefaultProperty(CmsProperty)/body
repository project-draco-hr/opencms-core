{
  if (OpenCms.getLog(this).isDebugEnabled()) {
    OpenCms.getLog(this).debug("addDefaultProperty(CmsProperty) called on " + this + " with property="+ property);
  }
  if (m_frozen) {
    throw new CmsConfigurationException("Resource type " + this.getClass().getName() + " with name='"+ getTypeName()+ "' id='"+ getTypeId()+ "' can't be reconfigured");
  }
  m_defaultProperties.add(property);
}
