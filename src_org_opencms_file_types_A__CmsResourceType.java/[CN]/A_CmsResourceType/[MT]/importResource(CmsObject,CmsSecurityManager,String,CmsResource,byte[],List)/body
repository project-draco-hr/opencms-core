{
  resource.setDateLastModified(resource.getDateLastModified());
  resource.setState(I_CmsConstants.C_STATE_NEW);
  return securityManager.createResource(cms.getRequestContext(),cms.getRequestContext().addSiteRoot(resourcename),resource,content,properties,true);
}
