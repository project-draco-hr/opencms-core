{
  if (isEnabled()) {
    String onClic="listMultiAction('" + getListId() + "','"+ getId()+ "', '"+ CmsStringUtil.escapeJavaScript(wp.resolveMacros(getConfirmationMessage().key(wp.getLocale())))+ "', '"+ CmsStringUtil.escapeJavaScript(wp.resolveMacros(Messages.get().key(wp.getLocale(),Messages.GUI_LIST_ACTION_NO_SELECTION_0,null)))+ "');";
    return A_CmsHtmlIconButton.defaultButtonHtml(CmsHtmlIconButtonStyleEnum.SMALL_ICON_TEXT,getId(),getName().key(wp.getLocale()),getHelpText().key(wp.getLocale()),isEnabled(),getIconPath(),onClic);
  }
  return "";
}
