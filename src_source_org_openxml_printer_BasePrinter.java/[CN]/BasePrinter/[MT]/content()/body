{
  ElementState state;
  state=getElementState();
  if (state != null) {
    if (state.empty) {
      printText(">");
      state.empty=false;
    }
    state.afterElement=false;
  }
  return state;
}
