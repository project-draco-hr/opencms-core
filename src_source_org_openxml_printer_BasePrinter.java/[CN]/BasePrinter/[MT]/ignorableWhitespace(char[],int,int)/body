{
  int i;
  content();
  if (_format.getIndenting()) {
    _thisIndent=0;
    for (i=start; i < end; ++i) {
      if (chars[i] == '\n' || chars[i] == '\r')       breakLine();
 else       _text.append(chars[i]);
    }
  }
}
