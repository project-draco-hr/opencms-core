{
  Tokenizer toke=new Tokenizer(str);
  str=toke.nextToken();
  m_publishTagFrom=Integer.parseInt(str);
  str=toke.nextToken();
  if ("null".equals(str)) {
    m_resourceId=null;
  }
 else {
    m_resourceId=str;
  }
}
