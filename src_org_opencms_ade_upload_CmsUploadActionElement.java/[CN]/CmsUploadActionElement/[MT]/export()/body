{
  StringBuffer sb=new StringBuffer();
  String prefetchedData=exportDictionary(CmsUploadData.DICT_NAME,I_CmsUploadService.class.getMethod("prefetch"),getUploadData());
  sb.append(prefetchedData);
  sb.append(ClientMessages.get().export(getRequest()));
  return sb.toString();
}
