{
  if (getGalleryItem().getIcon() != null) {
    return getGalleryItem().getIcon();
  }
  return CmsWorkplace.getResourceUri(CmsWorkplace.RES_PATH_FILETYPES + OpenCms.getWorkplaceManager().getExplorerTypeSetting(getType()).getIcon());
}
