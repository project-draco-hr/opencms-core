{
  if (getItem() != null) {
    String groupId=getItem().getId();
    try {
      CmsGroup group=getCms().readGroup(new CmsUUID(groupId));
      if (group.getDisabled()) {
        return getFirstAction();
      }
    }
 catch (    CmsException e) {
    }
  }
  return getSecondAction();
}
