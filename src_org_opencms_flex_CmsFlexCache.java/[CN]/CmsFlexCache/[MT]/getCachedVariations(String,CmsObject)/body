{
  if (!isEnabled()) {
    return null;
  }
  if (!isAdmin(cms)) {
    return null;
  }
  Object o=m_keyCache.get(key);
  if (o != null) {
    CmsFlexCacheVariation v=(CmsFlexCacheVariation)o;
    return v.m_map.keySet();
  }
  return null;
}
