{
  if (!isEnabled()) {
    return;
  }
  if (!cms.isAdmin()) {
    return;
  }
  if (DEBUG > 0) {
    System.err.println("FlexCache: Clearing offline entries");
  }
  clearAccordingToSuffix(C_CACHE_OFFLINESUFFIX,true);
}
