{
  if (!CmsCoreProvider.get().isKeepAlive()) {
    return;
  }
  if (INSTANCE == null) {
    m_keepRunning=true;
    CmsPingTimer timer=new CmsPingTimer();
    timer.run();
    INSTANCE=timer;
  }
}
