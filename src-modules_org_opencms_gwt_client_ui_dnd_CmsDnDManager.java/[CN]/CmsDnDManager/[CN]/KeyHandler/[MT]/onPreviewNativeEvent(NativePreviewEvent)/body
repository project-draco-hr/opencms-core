{
  if (!m_dragging) {
    return;
  }
  Event nativeEvent;
  try {
    nativeEvent=Event.as(e.getNativeEvent());
  }
 catch (  Exception exc) {
    return;
  }
  if (e.getTypeInt() != Event.ONKEYDOWN) {
    return;
  }
  if (nativeEvent.getKeyCode() == 27) {
    cancel();
  }
}
