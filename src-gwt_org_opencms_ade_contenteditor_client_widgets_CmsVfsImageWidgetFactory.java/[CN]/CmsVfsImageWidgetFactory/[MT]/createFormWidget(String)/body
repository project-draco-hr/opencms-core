{
  CmsImageGalleryWidget widget=new CmsImageGalleryWidget(I_CmsImageBundle.INSTANCE.style().imageSearchIcon(),configuration);
  widget.getElement().setAttribute("style","min-height:110px;");
  return new FormWidgetWrapper(widget);
}
