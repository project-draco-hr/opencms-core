{
  if (typeConfig.isAddDisabled()) {
    return true;
  }
  boolean matchingView=elementView.equals(typeConfig.getElementView()) || (typeConfig.isShowInDefaultView() && elementView.equals(CmsElementView.DEFAULT_ELEMENT_VIEW.getId()));
  return !matchingView;
}
