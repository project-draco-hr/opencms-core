{
  if (isAddDisabled()) {
    return true;
  }
  boolean matchingView=elementView.equals(getElementView()) || (isShowInDefaultView() && elementView.equals(CmsElementView.DEFAULT_ELEMENT_VIEW.getId()));
  return !matchingView;
}
