{
  CmsSitemapController controller=getHoverbar().getController();
  CmsClientSitemapEntry entry=controller.getEntry(getHoverbar().getSitePath());
  boolean show=(entry != null) && !entry.isNew() && entry.isEditable();
  setVisible(show);
}
