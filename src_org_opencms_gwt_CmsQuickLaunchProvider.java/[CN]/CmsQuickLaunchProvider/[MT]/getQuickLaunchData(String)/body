{
  List<CmsQuickLaunchData> result=Lists.newArrayList();
  for (  I_CmsWorkplaceAppConfiguration config : OpenCms.getWorkplaceAppManager().getQuickLaunchConfigurations(m_cms)) {
    if (config instanceof I_CmsHasADEQuickLaunchData) {
      Optional<CmsQuickLaunchData> optItem=((I_CmsHasADEQuickLaunchData)config).getADEQuickLaunchData(m_cms,context);
      if (optItem.isPresent()) {
        result.add(optItem.get());
      }
    }
  }
  return result;
}
