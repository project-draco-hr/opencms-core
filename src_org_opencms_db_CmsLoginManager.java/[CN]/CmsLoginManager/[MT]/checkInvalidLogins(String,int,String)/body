{
  if (m_maxBadAttempts < 0) {
    return;
  }
  String key=createStorageKey(userName,type,remoteAddress);
  CmsUserData userData=(CmsUserData)m_storage.get(key);
  if ((userData != null) && (userData.isDisabled())) {
    throw new CmsAuthentificationException(Messages.get().container(Messages.ERR_LOGIN_FAILED_TEMP_DISABLED_5,new Object[]{userName,new Integer(type),remoteAddress,userData.getReleaseDate(),userData.getInvalidLoginCount()}));
  }
}
