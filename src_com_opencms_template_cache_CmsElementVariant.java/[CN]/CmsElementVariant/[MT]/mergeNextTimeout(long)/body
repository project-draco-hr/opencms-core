{
  if (m_nextTimeout == 0 || timeout == 0) {
    if (m_nextTimeout < timeout) {
      m_nextTimeout=timeout;
    }
  }
 else {
    if (m_nextTimeout > timeout) {
      m_nextTimeout=timeout;
    }
  }
}
