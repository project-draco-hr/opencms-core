{
  String path=CmsJspNavBuilder.getDefaultFile(getCmsObject(),CmsResource.getFolderPath(getRequestContext().getUri()));
  if (path != null) {
    return path.equals(getRequestContext().getUri());
  }
  return false;
}
