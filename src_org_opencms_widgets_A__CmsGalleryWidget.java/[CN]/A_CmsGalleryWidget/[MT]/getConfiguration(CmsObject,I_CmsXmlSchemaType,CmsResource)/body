{
  CmsMessages messages=new CmsMessages(resource.getName(),"en");
  CmsGalleryWidgetConfiguration config=new CmsGalleryWidgetConfiguration(cms,messages,null,getConfiguration());
  String result=config.getConfigString();
  return result;
}
