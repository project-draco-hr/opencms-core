{
  String msg=error.getLocalizedMessage();
  if (error instanceof CmsException) {
    msg=((CmsException)error).getLocalizedMessage(OpenCms.getWorkplaceManager().getWorkplaceLocale(getCmsObject()));
  }
 else   if (error instanceof CmsRuntimeException) {
    msg=((CmsRuntimeException)error).getLocalizedMessage(OpenCms.getWorkplaceManager().getWorkplaceLocale(getCmsObject()));
  }
  result.put(JsonResponse.ERROR.getName(),msg);
  LOG.error(error.getMessage(),error);
}
