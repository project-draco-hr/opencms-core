{
  if (getDialog().getGalleryMode() == GalleryMode.editor) {
    CmsPreviewUtil.enableEditorOk(false);
  }
  CmsGalleryDialog.getPreviewParent(m_previewParentId).addStyleName(I_CmsLayoutBundle.INSTANCE.previewDialogCss().hidePreview());
}
