{
  boolean result;
  try {
    result=isProxyPublicCacheable(cms,templateFile,elementName,parameters,templateSelector) && cms.getRequestContext().currentUser().equals(cms.anonymousUser());
  }
 catch (  Exception e) {
    result=false;
  }
  return result;
}
