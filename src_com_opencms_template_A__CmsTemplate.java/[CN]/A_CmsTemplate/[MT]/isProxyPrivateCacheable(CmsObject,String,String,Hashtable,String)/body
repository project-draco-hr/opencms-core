{
  boolean result;
  try {
    result=isProxyPublicCacheable(cms,templateFile,elementName,parameters,templateSelector) && cms.getRequestContext().currentUser().isGuestUser();
  }
 catch (  Exception e) {
    result=false;
  }
  return result;
}
