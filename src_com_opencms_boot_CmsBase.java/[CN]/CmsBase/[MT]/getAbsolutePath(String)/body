{
  if (s == null) {
    return null;
  }
  File f=new File(s);
  if (!f.isAbsolute()) {
    if (c_basePath == null) {
      return null;
    }
 else {
      f=new File(c_basePath,s);
    }
  }
  return f.getAbsolutePath();
}
