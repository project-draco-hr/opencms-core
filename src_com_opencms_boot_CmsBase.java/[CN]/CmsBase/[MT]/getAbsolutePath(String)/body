{
  if (s == null) {
    return null;
  }
  File f=new File(s);
  if (!f.isAbsolute()) {
    if (OpenCms.getBasePath() == null) {
      return null;
    }
 else {
      return OpenCms.getBasePath() + s;
    }
  }
 else {
    return s;
  }
}
