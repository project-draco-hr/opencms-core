{
  if (s == null) {
    return null;
  }
  File f=new File(s);
  if (!f.isAbsolute()) {
    if (c_basePath == null) {
      return null;
    }
 else {
      f=new File(getWebBasePath() + s);
    }
  }
  return f.getAbsolutePath();
}
