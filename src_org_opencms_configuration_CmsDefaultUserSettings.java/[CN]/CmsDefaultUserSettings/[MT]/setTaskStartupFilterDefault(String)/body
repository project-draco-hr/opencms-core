{
  int defaultFilter=0;
  try {
    if (filter != null) {
      defaultFilter=FILTER_NAMES_LIST.indexOf(filter);
    }
  }
 catch (  Exception e) {
  }
  setTaskStartupFilter(FILTER_VALUES[defaultFilter]);
}
