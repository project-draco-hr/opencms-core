{
  if (!m_handler.isVisible(wp.getCms())) {
    return "";
  }
  String link=OpenCms.getWorkplaceManager().getToolManager().cmsLinkForPath(wp.getJsp(),this.getHandler().getPath(),null);
  String onClic="openPage('" + link + "');";
  return A_CmsHtmlIconButton.defaultBigButtonHtml(getId(),m_handler.getName(),m_handler.getHelpText(),m_handler.isEnabled(wp.getCms()),m_handler.getIconPath(),onClic);
}
