{
  if (!isVisible()) {
    return "";
  }
  String link=OpenCms.getWorkplaceManager().getToolManager().cmsLinkFromContext(wp.getJsp(),this);
  String onClic="openPage('" + link + "');";
  return A_CmsHtmlIconButton.defaultBigButtonHtml(getId(),getName(),getHelpText(),isEnabled(),getIconPath(),onClic);
}
