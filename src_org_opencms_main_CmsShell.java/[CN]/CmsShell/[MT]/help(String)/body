{
  String commandList;
  boolean foundSomething=false;
  System.out.println();
  Iterator i=m_commandObjects.iterator();
  while (i.hasNext()) {
    CmsCommandObject cmdObj=(CmsCommandObject)i.next();
    commandList=cmdObj.getMethodHelp(searchString);
    if (!CmsStringUtil.isEmpty(commandList)) {
      System.out.println(Messages.get().key(getLocale(),Messages.GUI_SHELL_AVAILABLE_METHODS_1,new Object[]{cmdObj.getObject().getClass().getName()}));
      System.out.println(commandList);
      foundSomething=true;
    }
  }
  if (!foundSomething) {
    System.out.println(Messages.get().key(getLocale(),Messages.GUI_SHELL_MATCH_SEARCHSTRING_1,new Object[]{searchString}));
  }
}
