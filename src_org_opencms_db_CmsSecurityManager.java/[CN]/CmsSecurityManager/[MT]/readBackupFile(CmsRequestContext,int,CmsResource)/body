{
  CmsBackupResource result=null;
  CmsDbContext dbc=m_dbContextFactory.getDbContext(context);
  try {
    result=m_driverManager.readBackupFile(dbc,tagId,resource);
  }
 catch (  Exception e) {
    dbc.report(null,Messages.get().container(Messages.ERR_READ_BKP_FILE_2,dbc.getRequestContext().removeSiteRoot(resource.getRootPath()),String.valueOf(tagId)),e);
  }
 finally {
    dbc.clear();
  }
  return result;
}
