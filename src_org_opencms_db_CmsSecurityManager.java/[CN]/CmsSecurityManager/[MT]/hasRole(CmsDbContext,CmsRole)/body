{
  List groups;
  try {
    groups=m_driverManager.getGroupsOfUser(dbc,dbc.currentUser().getName(),dbc.getRequestContext().getRemoteAddress());
  }
 catch (  CmsException e) {
    return false;
  }
  return roles.hasRole(groups);
}
