{
  CmsDbContext dbc=m_dbContextFactory.getDbContext(context);
  try {
    m_driverManager.backupProject(dbc,tagId,publishDate);
  }
 catch (  Exception e) {
    CmsMessageContainer errMsg=Messages.get().container(Messages.ERR_BACKUP_PROJECT_4,new Object[]{new Integer(tagId),dbc.currentProject().getName(),new Integer(dbc.currentProject().getId()),new Long(publishDate)});
    dbc.report(null,errMsg,e);
  }
 finally {
    dbc.clear();
  }
}
