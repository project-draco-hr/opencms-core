{
  CmsDbContext dbc=m_dbContextFactory.getDbContext(context);
  List result=null;
  try {
    result=m_driverManager.readResourcesWithProperty(dbc,path,propertyDefinition);
  }
 catch (  Exception e) {
    dbc.report(null,Messages.get().container(Messages.ERR_READ_RESOURCES_FOR_PROP_SET_2,path,propertyDefinition),e);
  }
 finally {
    dbc.clear();
  }
  return result;
}
