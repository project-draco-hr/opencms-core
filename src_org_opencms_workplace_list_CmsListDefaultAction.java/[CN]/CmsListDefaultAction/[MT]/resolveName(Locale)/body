{
  if (getColumnForLink() == null) {
    return super.resolveName(locale);
  }
  return (getItem().get(getColumnForLink()) != null) ? getItem().get(getColumnForLink()).toString() : getName().key(locale);
}
