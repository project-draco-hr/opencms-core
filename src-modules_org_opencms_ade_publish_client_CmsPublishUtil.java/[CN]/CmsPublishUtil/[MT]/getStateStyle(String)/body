{
  String result=CSS.noState();
switch (code.charAt(0)) {
case 'N':
    result=CSS.stateNew();
  break;
case 'D':
result=CSS.stateDeleted();
break;
case 'C':
result=CSS.stateChanged();
break;
default :
break;
}
return result;
}
