def __init__(self, environ=None):
    'Constructor: Here you should parse request fields, initialize variables, etc.'
    self.request = FCKeditorRequest(environ)
    self.headers = []
    if environ:
        self.environ = environ
    else:
        self.environ = os.environ
