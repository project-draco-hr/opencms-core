{
  if (title.equals(CmsSiteManagerImpl.SHARED_FOLDER_TITLE)) {
    return Messages.get().getBundle(getSettings().getUserSettings().getLocale()).key(Messages.GUI_SHARED_TITLE_0);
  }
  return title;
}
