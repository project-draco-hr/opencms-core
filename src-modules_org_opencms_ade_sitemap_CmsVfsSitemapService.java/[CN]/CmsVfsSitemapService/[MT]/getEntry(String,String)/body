{
  CmsClientSitemapEntry result=null;
  try {
    CmsObject cms=getCmsObject();
    Map<String,CmsXmlContentProperty> propertyConfig=OpenCms.getSitemapManager().getElementPropertyConfiguration(cms,root);
    result=toClientEntry(getNavBuilder().getNavigationForResource(root),propertyConfig,false);
  }
 catch (  Throwable e) {
    error(e);
  }
  return result;
}
