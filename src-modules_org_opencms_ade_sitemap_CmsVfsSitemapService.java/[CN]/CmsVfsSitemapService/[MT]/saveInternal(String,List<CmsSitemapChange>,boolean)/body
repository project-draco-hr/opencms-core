{
  for (  CmsSitemapChange change : changes) {
    if (!change.isDelete()) {
      applyChange(sitemapUri,change);
    }
 else {
      removeEntryFromNavigation(change);
    }
  }
  return System.currentTimeMillis();
}
