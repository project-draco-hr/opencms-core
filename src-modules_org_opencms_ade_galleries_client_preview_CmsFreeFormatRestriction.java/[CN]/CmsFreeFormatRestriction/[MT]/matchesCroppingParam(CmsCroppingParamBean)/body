{
  return ((croppingParam.getCropX() != I_CmsFormatRestriction.DIMENSION_NOT_SET) && (croppingParam.getTargetHeight() == croppingParam.getCropHeight())) && (croppingParam.getTargetWidth() == croppingParam.getCropWidth());
}
