{
  if (term == null) {
    return "";
  }
  StringBuffer result=new StringBuffer(term.length() + 16);
  result.append("<b>");
  result.append(term);
  result.append("</b>");
  return result.toString();
}
