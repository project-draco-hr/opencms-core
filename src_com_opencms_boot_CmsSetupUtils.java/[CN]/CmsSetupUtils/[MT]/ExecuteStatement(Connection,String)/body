{
  Statement stat;
  if (statement != null) {
    try {
      stat=con.createStatement();
      stat.executeUpdate(statement);
    }
 catch (    Exception e) {
      if (m_errorLogging) {
        CmsSetup.setErrors(e.toString() + "\n");
      }
    }
  }
}
