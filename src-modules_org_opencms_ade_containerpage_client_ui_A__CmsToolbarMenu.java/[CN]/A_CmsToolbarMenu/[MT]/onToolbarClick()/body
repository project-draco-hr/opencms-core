{
  boolean active=isActive();
  if (!active) {
    m_handler.deactivateCurrentButton();
    m_handler.setActiveButton(this);
    onToolbarActivate();
    openMenu();
  }
 else {
    m_handler.deactivateCurrentButton();
  }
}
