{
  CmsContainerPageElementPanel dragElement=new CmsContainerPageElementPanel(element,dragParent,clientId,sitePath,noEditReason,hasProps,hasViewPermission,releasedAndNotExpired);
  addOptionBar(dragElement);
  return dragElement;
}
