{
  CmsContainerPageElement dragElement=new CmsContainerPageElement(element,dragParent,clientId,sitePath,noEditReason,hasProps,hasViewPermission,releasedAndNotExpired);
  addOptionBar(dragElement);
  return dragElement;
}
