{
  CmsContainerPageElement dragElement=new CmsContainerPageElement(element,dragParent,clientId,sitePath,noEditReason,hasProps,hasViewPermission);
  addOptionBar(dragElement);
  return dragElement;
}
