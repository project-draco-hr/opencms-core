{
  if (!isVisible()) {
    return "";
  }
  return A_CmsHtmlIconButton.defaultButtonHtml(wp.getJsp(),resolveButtonStyle(),getId() + getItem().getId(),getId(),resolveName(wp.getLocale()),resolveHelpText(wp.getLocale()),isEnabled(),getIconPath(),resolveOnClic(wp.getLocale()),getColumnForTexts() == null);
}
