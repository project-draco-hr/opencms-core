{
  ByteArrayOutputStream out=new ByteArrayOutputStream(512);
  int c=0;
  while (true) {
    try {
      c=in.read();
      if (c < 0) {
        break;
      }
      out.write(c);
    }
 catch (    IOException e) {
      break;
    }
  }
  return extractText(out.toByteArray(),encoding);
}
