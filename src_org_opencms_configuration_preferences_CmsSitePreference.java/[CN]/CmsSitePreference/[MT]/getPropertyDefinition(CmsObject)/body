{
  Locale locale=OpenCms.getWorkplaceManager().getWorkplaceLocale(cms);
  String options=getSiteSelectOptionsStatic(cms,locale);
  CmsXmlContentProperty prop=new CmsXmlContentProperty(getName(),"string","select_notnull",options,null,null,null,NICE_NAME,null,null,null);
  return prop;
}
