{
  Locale locale=OpenCms.getWorkplaceManager().getWorkplaceLocale(cms);
  SelectOptions options=CmsPreferences.getSiteSelectOptionsStatic(cms,"DUMMY SITE VALUE",locale);
  CmsXmlContentProperty prop=new CmsXmlContentProperty(getName(),"string","select_notnull",options.toClientSelectWidgetConfiguration(),null,null,null,NICE_NAME,null,null,null);
  return prop;
}
