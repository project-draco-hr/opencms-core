{
  if (resource == null) {
    LOG.warn(Messages.get().container(Messages.LOG_WARN_UNCACHE_NULL_0));
    return;
  }
  if (!m_useMemoryMonitor || m_online) {
    return;
  }
  if (resource.getRootPath().equals(CmsResourceTypeXmlSitemap.SCHEMA)) {
    m_defProps=null;
  }
  if (!CmsResourceTypeXmlSitemap.isSitemap(resource) && !resource.getRootPath().equals(CmsResourceTypeXmlSitemap.SCHEMA)) {
    return;
  }
  if (m_active != null) {
    m_active.clear();
    m_active=null;
  }
}
