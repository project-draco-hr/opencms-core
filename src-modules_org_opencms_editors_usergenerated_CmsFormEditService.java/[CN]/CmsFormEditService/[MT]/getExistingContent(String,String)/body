{
  CmsFormContent formContent=null;
  try {
    CmsFormSession session=CmsFormSessionFactory.getInstance().createSession(getCmsObject(),getRequest(),configPath);
    formContent=readContent(session,session.loadXmlContent(sitepath));
  }
 catch (  Exception e) {
    error(e);
  }
  return formContent;
}
