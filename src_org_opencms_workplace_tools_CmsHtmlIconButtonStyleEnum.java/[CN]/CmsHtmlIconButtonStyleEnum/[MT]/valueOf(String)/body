{
  Iterator iter=VALUES.iterator();
  while (iter.hasNext()) {
    CmsHtmlIconButtonStyleEnum target=(CmsHtmlIconButtonStyleEnum)iter.next();
    if (value == target.getStyle()) {
      return target;
    }
  }
  throw new IllegalArgumentException(Messages.get().key(Messages.ERR_LIST_ENUM_PARSE_2,new Integer(value),CmsHtmlIconButtonStyleEnum.class.getName()));
}
