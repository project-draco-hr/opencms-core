{
  if ((m_userAgent.indexOf(devicePalm) != -1) || (m_userAgent.indexOf(engineBlazer) != -1) || (m_userAgent.indexOf(engineXiino) != -1)) {
    if (detectPalmWebOS()) {
      return false;
    }
 else {
      return true;
    }
  }
  return false;
}
