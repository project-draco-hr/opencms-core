{
  if (m_userAgent.indexOf(maemo) != -1) {
    return true;
  }
 else   if ((m_userAgent.indexOf(linux) != -1) && (m_userAgent.indexOf(deviceTablet) != -1) && !detectWebOSTablet()&& !detectAndroid()) {
    return true;
  }
  return false;
}
