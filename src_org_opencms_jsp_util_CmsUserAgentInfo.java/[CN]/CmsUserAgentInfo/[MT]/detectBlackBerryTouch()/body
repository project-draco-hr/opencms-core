{
  if (detectBlackBerry() && ((m_userAgent.indexOf(deviceBBStorm) != -1) || (m_userAgent.indexOf(deviceBBTorch) != -1) || (m_userAgent.indexOf(deviceBBBoldTouch) != -1)|| (m_userAgent.indexOf(deviceBBCurveTouch) != -1))) {
    return true;
  }
  return false;
}
