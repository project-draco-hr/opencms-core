{
  if (!m_keyset.equals(mode)) {
    try {
      adjustExistingContainer(m_locale,mode);
    }
 catch (    IOException|CmsException e) {
      e.printStackTrace();
    }
    m_keysetMode=mode;
  }
}
