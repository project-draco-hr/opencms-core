{
  super();
  m_textBox.addStyleName(CSS.textBox());
  FlowPanel panel=new FlowPanel();
  panel.addStyleName(CSS.textBoxWidget());
  panel.add(m_textBox);
  panel.add(m_error);
  initWidget(panel);
}
