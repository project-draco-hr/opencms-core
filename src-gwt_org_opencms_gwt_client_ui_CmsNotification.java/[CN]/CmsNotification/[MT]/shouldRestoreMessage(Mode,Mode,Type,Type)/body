{
  if (!Mode.STICKY.equals(oldMode)) {
    return false;
  }
  if (!Mode.STICKY.equals(newMode)) {
    return true;
  }
  if (isSuperiorType(oldType,newType)) {
    return false;
  }
  return true;
}
