{
  if (cache.size() < max_objects) {
    cache.put(strKey,new CmsCachedObject(value));
  }
 else {
    removeLRU();
    cache.put(strKey,new CmsCachedObject(value));
  }
  int id=getId(value);
  if (id != C_UNKNOWN_INT) {
    index.put(new Integer(id),strKey);
  }
}
