{
  PreparedStatement statement=null;
  try {
    statement=conn.prepareStatement("UPDATE CMS_SYSTEMID SET ID=? WHERE CMS_SYSTEMID.TABLE_KEY=? AND CMS_SYSTEMID.ID=?");
    statement.setInt(1,newId);
    statement.setString(2,tableName);
    statement.setInt(3,oldId);
    int amount=statement.executeUpdate();
    return (amount == 1);
  }
 catch (  SQLException e) {
    throw new CmsDbSqlException(CmsDbUtil.class.getName(),statement,e);
  }
 finally {
    if (statement != null) {
      try {
        statement.close();
      }
 catch (      SQLException exc) {
      }
    }
  }
}
