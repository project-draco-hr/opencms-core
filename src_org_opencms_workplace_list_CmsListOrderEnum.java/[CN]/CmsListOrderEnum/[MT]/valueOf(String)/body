{
  Iterator iter=VALUES.iterator();
  while (iter.hasNext()) {
    CmsListOrderEnum target=(CmsListOrderEnum)iter.next();
    if (value == target.getOrder()) {
      return target;
    }
  }
  throw new IllegalArgumentException(Messages.get().key(Messages.ERR_LIST_ENUM_PARSE_2,new Integer(value),CmsListOrderEnum.class.getName()));
}
