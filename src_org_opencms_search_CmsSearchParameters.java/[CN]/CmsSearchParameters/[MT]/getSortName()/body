{
  if (m_sort == SORT_DATE_CREATED) {
    return SORT_NAMES[1];
  }
  if (m_sort == SORT_DATE_LASTMODIFIED) {
    return SORT_NAMES[2];
  }
  if (m_sort == SORT_TITLE) {
    return SORT_NAMES[3];
  }
  return SORT_NAMES[0];
}
