{
  String elementClass=element.getClassName().trim();
  if (elementClass.contains(className) && (elementClass.equals(className) || elementClass.startsWith(className + " ") || elementClass.endsWith(" " + className)|| elementClass.contains(" " + className + " "))) {
    return true;
  }
  return false;
}
