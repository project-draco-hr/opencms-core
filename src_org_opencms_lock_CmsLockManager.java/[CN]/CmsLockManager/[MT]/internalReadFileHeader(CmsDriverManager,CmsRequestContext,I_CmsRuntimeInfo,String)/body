{
  CmsResource resource=null;
  try {
    resource=driverManager.getVfsDriver().readFileHeader(runtimeInfo,context.currentProject().getId(),resourcename,true);
  }
 catch (  CmsException e) {
    resource=null;
  }
  return resource;
}
