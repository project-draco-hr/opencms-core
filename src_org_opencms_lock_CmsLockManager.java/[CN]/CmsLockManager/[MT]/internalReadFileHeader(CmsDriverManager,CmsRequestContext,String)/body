{
  CmsResource resource=null;
  try {
    resource=driverManager.getVfsDriver().readFileHeader(context.currentProject().getId(),resourcename,true);
  }
 catch (  CmsException e) {
    resource=null;
  }
  return resource;
}
