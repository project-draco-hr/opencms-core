{
  if (CmsStringUtil.isEmptyOrWhitespaceOnly(getParamProjectid()) || CmsStringUtil.isEmptyOrWhitespaceOnly(getParamProjectname())) {
    setAction(ACTION_CANCEL);
    actionCloseDialog();
    return;
  }
  JspWriter out=getJsp().getJspContext().getOut();
  out.print(defaultActionHtml());
}
