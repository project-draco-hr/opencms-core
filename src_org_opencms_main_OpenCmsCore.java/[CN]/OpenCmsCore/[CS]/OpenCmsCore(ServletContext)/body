{
synchronized (this) {
synchronized (context) {
      if (m_instance != null) {
        throw new CmsInitException("OpenCms already initialized!");
      }
 else {
        m_instance=this;
        initMembers();
        init(context);
      }
    }
  }
}
