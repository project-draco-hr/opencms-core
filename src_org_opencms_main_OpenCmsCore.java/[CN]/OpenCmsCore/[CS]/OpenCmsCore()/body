{
synchronized (m_lock) {
    if (m_instance != null && (m_instance.getRunLevel() > 0)) {
      throw new CmsInitException("OpenCms already initialized!");
    }
    initMembers();
    setRunLevel(1);
    m_instance=this;
  }
}
