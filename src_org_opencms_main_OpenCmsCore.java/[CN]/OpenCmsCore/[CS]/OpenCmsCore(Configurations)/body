{
synchronized (this) {
synchronized (conf) {
      if (m_instance != null) {
        throw new CmsInitException("OpenCms already initialized!");
      }
 else {
        m_instance=this;
        try {
          initConfiguration(conf);
        }
 catch (        Exception e) {
        }
      }
    }
  }
}
