{
  CmsSite site=OpenCms.getSiteManager().matchRequest(request);
  return initCmsObject(request,cms.getRequestContext().currentUser(),site.getSiteRoot(),cms.getRequestContext().currentProject().getUuid(),cms.getRequestContext().getOuFqn());
}
