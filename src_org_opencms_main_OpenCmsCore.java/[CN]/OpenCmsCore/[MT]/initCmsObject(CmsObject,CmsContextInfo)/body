{
  String userName=contextInfo.getUserName();
  if (adminCms == null || !adminCms.isAdmin()) {
    if (!userName.equals(getDefaultUsers().getUserGuest()) && !userName.equals(getDefaultUsers().getUserExport())) {
      CmsMessageContainer message=Messages.get().container(Messages.ERR_INVALID_INIT_USER_2,userName,((adminCms != null) ? (adminCms.getRequestContext().currentUser().getName()) : ""));
      if (LOG.isWarnEnabled()) {
        LOG.warn(message);
      }
      throw new CmsRoleViolationException(message);
    }
  }
  return initCmsObject(contextInfo);
}
