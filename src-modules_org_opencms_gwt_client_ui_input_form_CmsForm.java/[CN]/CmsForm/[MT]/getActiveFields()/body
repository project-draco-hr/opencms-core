{
  List<I_CmsFormField> result=new ArrayList<I_CmsFormField>();
  for (  I_CmsFormField field : m_fields.values()) {
    if (!field.isIgnored()) {
      result.add(field);
    }
  }
  return result;
}
