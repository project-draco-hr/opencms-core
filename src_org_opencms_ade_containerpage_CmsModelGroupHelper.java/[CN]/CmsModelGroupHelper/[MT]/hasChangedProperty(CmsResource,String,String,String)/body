{
  boolean propsChanged=false;
  if (!title.equals(m_cms.readPropertyObject(modelGroup,CmsPropertyDefinition.PROPERTY_TITLE,false).getValue())) {
    propsChanged=true;
  }
 else   if (!description.equals(m_cms.readPropertyObject(modelGroup,CmsPropertyDefinition.PROPERTY_DESCRIPTION,false).getValue())) {
    propsChanged=true;
  }
 else   if (!groupType.equals(m_cms.readPropertyObject(modelGroup,CmsPropertyDefinition.PROPERTY_TEMPLATE_ELEMENTS,false).getValue())) {
    propsChanged=true;
  }
  return propsChanged;
}
