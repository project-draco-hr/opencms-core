{
  storeEventPos(event);
  if (!m_dragging) {
    return;
  }
  if (!canDropNow()) {
    cancelDragging();
    return;
  }
switch (event.getNativeButton()) {
case NativeEvent.BUTTON_LEFT:
    stopDragging(event);
  break;
default :
cancelDragging();
}
}
