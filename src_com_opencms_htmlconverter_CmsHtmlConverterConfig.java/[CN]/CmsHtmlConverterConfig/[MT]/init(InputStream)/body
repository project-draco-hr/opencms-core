{
  Node node;
  Tidy tidy=new Tidy();
  tidy.setXmlTags(true);
  tidy.setQuiet(true);
  tidy.setOnlyErrors(true);
  tidy.setShowWarnings(false);
  if (m_isConfigured) {
    clearConfiguration();
  }
  node=tidy.parseDOM(in,null);
  parseConfig(node);
  buildObjects();
  m_isConfigured=true;
}
