{
  Object o=session.getAttribute(SESSION_ATTRIBUTE);
  if ((o != null) && (o instanceof CmsCKEditorConfiguration)) {
    session.removeAttribute(SESSION_ATTRIBUTE);
    return (CmsCKEditorConfiguration)o;
  }
  return new CmsCKEditorConfiguration();
}
