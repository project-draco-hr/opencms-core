{
  CmsClientSitemapEntry entry=getHoverbar().getEntry();
  boolean show=!CmsSitemapView.getInstance().isGalleryMode() && (entry != null) && (entry.getEntryType() == EntryType.redirect);
  setVisible(show);
}
