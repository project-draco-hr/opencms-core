{
  CmsClientSitemapEntry entry=getHoverbar().getEntry();
  boolean show=!CmsSitemapView.getInstance().isSpecialMode() && (entry != null) && (entry.getEntryType() == EntryType.redirect);
  setVisible(show);
}
