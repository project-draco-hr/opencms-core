{
  boolean active=isActive();
  if (!active) {
    getHandler().deactivateCurrentButton();
    getHandler().setActiveButton(this);
    setDown(true);
    onToolbarActivate();
  }
 else {
    getHandler().deactivateCurrentButton();
  }
}
