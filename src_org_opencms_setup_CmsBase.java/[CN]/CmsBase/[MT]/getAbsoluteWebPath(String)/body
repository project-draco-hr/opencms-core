{
  if (s == null) {
    return null;
  }
  File f=new File(s);
  if (!f.isAbsolute()) {
    if (OpenCms.getSystemInfo().getBasePath() == null) {
      return null;
    }
 else {
      return getWebBasePath() + s;
    }
  }
 else {
    return s;
  }
}
