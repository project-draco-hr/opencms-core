{
  String dateAsString=m_dateBox.getBox().getText().trim();
  Date date=null;
  try {
    date=CmsDateConverter.toDate(dateAsString);
    m_dateBox.getBox().setErrorMessage(null);
  }
 catch (  Exception e) {
    m_dateBox.getBox().setErrorMessage(Messages.get().key(Messages.ERR_DATEBOX_INVALID_DATE_FORMAT_1,CmsDateConverter.cutSuffix(dateAsString)));
  }
  return date;
}
