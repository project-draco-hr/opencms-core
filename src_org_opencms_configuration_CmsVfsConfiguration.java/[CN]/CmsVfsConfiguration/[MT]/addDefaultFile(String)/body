{
  m_defaultFiles.add(defaultFile);
  if (CmsLog.LOG.isInfoEnabled()) {
    CmsLog.LOG.info(Messages.get().key(Messages.INIT_VFS_DEFAULT_FILE_2,new Integer(m_defaultFiles.size()),defaultFile));
  }
}
