{
  if (getCms().hasPublishPermissions(getParamResourcePath())) {
    return button("javascript:publishResource(\'" + getParamResourcePath() + "\');",null,"publish.png","messagebox.title.publishresource",0);
  }
  return button(null,null,"publish_in.png","",0);
}
