{
  for (int i=0; i < array.length; i++) {
    if (array[i] == null) {
      if (item == null) {
        return i;
      }
    }
 else {
      if ((item != null) && array[i].trim().equals(item.trim())) {
        return i;
      }
    }
  }
  return -1;
}
