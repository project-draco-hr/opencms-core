{
  ServiceLoader<I_CmsContextMenuItemProvider> providerLoader=ServiceLoader.load(I_CmsContextMenuItemProvider.class);
  for (  I_CmsContextMenuItemProvider provider : providerLoader) {
    addProvider(provider);
  }
}
