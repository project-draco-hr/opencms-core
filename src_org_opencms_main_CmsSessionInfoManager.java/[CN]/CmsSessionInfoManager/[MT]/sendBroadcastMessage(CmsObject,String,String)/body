{
synchronized (m_sessions) {
    CmsSessionInfo sessionInfo=(CmsSessionInfo)m_sessions.get(sessionId);
    if (sessionInfo != null) {
      sessionInfo.getBroadcastMessageQueue().addBroadcastMessage(new CmsBroadcastMessage(cms.getRequestContext().currentUser(),message));
    }
  }
}
