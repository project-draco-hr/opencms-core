{
  if (!test) {
    return TESTPARAM;
  }
  final String param=parseOptionalStringValue(XML_ELEMENT_QUERYPARAM);
  if (param == null) {
    return DEFAULT_QUERY_PARAM;
  }
 else {
    return param;
  }
}
