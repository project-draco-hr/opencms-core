{
  if (obj instanceof CmsLruCache)   return Integer.toString(((CmsLruCache)obj).size());
  if (obj instanceof CmsLruHashMap)   return Integer.toString(((CmsLruHashMap)obj).size());
  if (obj instanceof Map)   return Integer.toString(((Map)obj).size());
  return "-";
}
