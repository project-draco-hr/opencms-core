{
  if (obj instanceof CmsLruCache)   return Integer.toString(((CmsLruCache)obj).getMaxCacheCosts());
  if (obj instanceof CmsLruHashMap)   return Integer.toString(((CmsLruHashMap)obj).getLruCache().getMaxCacheCosts());
  return "-";
}
