{
  if (obj instanceof CmsLruCache)   return Integer.toString(((CmsLruCache)obj).getObjectCosts());
  if (obj instanceof CmsLruHashMap)   return Integer.toString(((CmsLruHashMap)obj).getLruCache().getObjectCosts());
  return "-";
}
