{
  if (oldType == null) {
    return true;
  }
switch (newType) {
case ERROR:
    return !oldType.equals(Type.ERROR);
case NORMAL:
  return false;
case WARNING:
return oldType.equals(Type.NORMAL);
default :
}
return true;
}
