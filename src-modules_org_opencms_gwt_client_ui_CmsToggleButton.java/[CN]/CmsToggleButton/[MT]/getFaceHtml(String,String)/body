{
  String result=((imageClass != null) && (imageClass.trim().length() > 0)) ? "<span class='" + imageClass + "'></span>" : "";
  if ((text != null) && (text.trim().length() > 0)) {
    result+=(result.length() > 0) ? "&nbsp;" : "";
    result+=text.trim();
  }
  return result;
}
