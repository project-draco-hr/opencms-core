{
  Iterator itUsers=CmsStringUtil.splitAsList(getJsp().getRequest().getParameter(A_CmsEditUserDialog.PARAM_USERID),CmsHtmlList.ITEM_SEPARATOR,true).iterator();
  while (itUsers.hasNext()) {
    CmsUUID id=new CmsUUID(itUsers.next().toString());
    getCms().deleteWebUser(id);
  }
  refreshList();
  actionCloseDialog();
}
