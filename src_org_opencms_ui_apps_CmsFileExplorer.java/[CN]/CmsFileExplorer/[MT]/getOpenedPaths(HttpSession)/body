{
  @SuppressWarnings("unchecked") Map<String,String> openedPaths=(Map<String,String>)session.getAttribute(OPENED_PATHS);
  if (openedPaths == null) {
    openedPaths=new HashMap<String,String>();
    session.setAttribute(OPENED_PATHS,openedPaths);
  }
  return openedPaths;
}
