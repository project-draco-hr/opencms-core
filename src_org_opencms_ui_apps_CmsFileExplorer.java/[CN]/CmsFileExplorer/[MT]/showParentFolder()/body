{
  CmsUUID parentId=(CmsUUID)((HierarchicalContainer)m_fileTree.getContainerDataSource()).getParent(m_currentFolder);
  if (parentId != null) {
    readFolder(parentId);
    m_fileTree.select(parentId);
  }
}
