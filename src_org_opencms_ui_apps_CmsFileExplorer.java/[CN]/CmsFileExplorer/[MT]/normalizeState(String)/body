{
  String result="";
  if (state.contains("|")) {
    if (!state.startsWith("|") && !state.startsWith("/")) {
      result="/" + state;
    }
 else {
      result=state;
      while (result.startsWith("//")) {
        result=result.substring(1);
      }
    }
  }
  return result;
}
