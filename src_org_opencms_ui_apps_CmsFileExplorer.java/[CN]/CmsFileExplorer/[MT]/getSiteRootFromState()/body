{
  String siteRoot=null;
  if (m_currentState.contains(STATE_SEPARATOR)) {
    siteRoot=m_currentState.substring(m_currentState.indexOf(STATE_SEPARATOR) + STATE_SEPARATOR.length(),m_currentState.lastIndexOf(STATE_SEPARATOR));
  }
  return siteRoot;
}
