{
  String preview=root.attributeValue(APPINFO_ATTR_URI);
  if (preview == null) {
    throw new CmsXmlException("Missing preview uri for element " + root.getName() + " in content definition "+ contentDefinition.getSchemaLocation());
  }
  m_previewLocation=preview;
}
