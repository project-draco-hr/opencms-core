{
  String name=root.attributeValue(APPINFO_ATTR_NAME);
  if (name == null) {
    throw new CmsXmlException("Missing resource bundle name for element " + root.getName() + " in content definition "+ contentDefinition.getSchemaLocation());
  }
  m_messageBundleName=name;
}
