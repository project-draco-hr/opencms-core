{
  CmsRpcAction<CmsContainerElement> action=new CmsRpcAction<CmsContainerElement>(){
    /** 
 * @see org.opencms.gwt.client.rpc.CmsRpcAction#execute()
 */
    @Override public void execute(){
      start(200);
      getContainerpageService().getElementWithProperties(CmsContainerpageController.getCurrentUri(),null,clientId,properties,m_containerTypes,this);
    }
    /** 
 * @see org.opencms.gwt.client.rpc.CmsRpcAction#onResponse(java.lang.Object)
 */
    @Override protected void onResponse(    CmsContainerElement result){
      callback.execute(result);
    }
  }
;
  action.execute();
}
