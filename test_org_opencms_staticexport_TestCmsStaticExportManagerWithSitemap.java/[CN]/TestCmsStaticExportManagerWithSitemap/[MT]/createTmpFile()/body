{
  boolean success=false;
  if (TMP_FILE.exists()) {
    deleteTmpFile();
  }
  if (!TMP_FILE.exists()) {
    TMP_FILE.getParentFile().mkdirs();
    success=TMP_FILE.createNewFile();
  }
  return success;
}
