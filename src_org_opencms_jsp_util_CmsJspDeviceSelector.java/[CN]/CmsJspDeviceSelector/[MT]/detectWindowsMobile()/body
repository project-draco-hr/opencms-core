{
  if ((m_userAgent.indexOf(DEVICE_WIN_MOBILE) != -1) || (m_userAgent.indexOf(DEVICE_IEMOBILE) != -1) || (m_userAgent.indexOf(ENGINE_PIE) != -1)|| (detectWapWml() && (m_userAgent.indexOf(DEVICE_WINDOWS) != -1))) {
    return true;
  }
  return false;
}
