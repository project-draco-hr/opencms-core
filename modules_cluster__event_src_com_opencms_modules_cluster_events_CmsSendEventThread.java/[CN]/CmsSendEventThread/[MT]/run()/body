{
  try {
    m_container.sendCommand(m_url);
  }
 catch (  IOException e) {
    if (I_CmsLogChannels.C_PREPROCESSOR_IS_LOGGING && A_OpenCms.isLogging()) {
      A_OpenCms.log(A_OpenCms.C_MODULE_DEBUG," module com.opencms.modules.cluster.events cant execute command " + e.getMessage());
    }
  }
}
