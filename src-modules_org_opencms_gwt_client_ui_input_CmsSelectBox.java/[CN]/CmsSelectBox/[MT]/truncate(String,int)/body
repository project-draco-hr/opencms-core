{
  m_openerLabel.truncate(textMetricsPrefix + TM_OPENER_LABEL,widgetWidth);
  for (  Widget widget : m_selector) {
    if (widget instanceof CmsLabel) {
      ((CmsLabel)widget).truncate(textMetricsPrefix + TM_OPTION,widgetWidth - 2 - 5);
    }
  }
}
