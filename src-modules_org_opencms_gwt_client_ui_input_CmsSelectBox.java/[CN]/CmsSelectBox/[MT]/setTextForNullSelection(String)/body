{
  CmsLabelSelectCell cell=m_selectCells.get("");
  if (cell == null) {
    return;
  }
  cell.setText(text);
  if (CmsStringUtil.isEmptyOrWhitespaceOnly(m_selectedValue)) {
    selectValue("");
  }
}
