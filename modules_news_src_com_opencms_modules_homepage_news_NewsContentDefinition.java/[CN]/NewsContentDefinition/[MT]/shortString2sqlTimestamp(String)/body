{
  DateFormat df;
  df=DateFormat.getDateInstance(DateFormat.MEDIUM,m_locale);
  java.util.Date uDate=df.parse(sDate);
  java.sql.Timestamp sqlTimestamp=new java.sql.Timestamp(uDate.getTime());
  return sqlTimestamp;
}
