{
  CmsObject cms=getCmsObject();
  CmsXmlSitemap xml=CmsXmlSitemapFactory.unmarshal(cms,sitemap);
  CmsClientSitemapEntry root=toClientEntry(xml.getSitemap(cms,cms.getRequestContext().getLocale()).getSiteEntries().get(0));
  root.setSubEntries(getChildren(root.getSitePath(),2));
  return root;
}
