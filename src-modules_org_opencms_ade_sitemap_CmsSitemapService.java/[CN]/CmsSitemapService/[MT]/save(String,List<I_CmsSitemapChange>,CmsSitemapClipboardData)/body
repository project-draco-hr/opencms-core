{
  long timestamp=0;
  try {
    timestamp=saveInternal(sitemapUri,changes,true);
    if (clipboardData != null) {
      setClipboardData(clipboardData);
    }
  }
 catch (  Throwable e) {
    error(e);
  }
  return timestamp;
}
