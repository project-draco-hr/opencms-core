{
  if ((a == null) || (b == null)) {
    assertTrue(a == b);
  }
  if (a != null) {
    assertEquals(a.length,b.length);
    for (int i=0; i < a.length; i++) {
      assertEquals(a[i],b[i]);
    }
  }
}
