{
  Iterator<String> i=strings.iterator();
  System.out.println("\nSize: " + strings.size() + "\n");
  while (i.hasNext()) {
    System.out.println(i.next());
  }
  System.out.println("\n--------------------------------");
}
