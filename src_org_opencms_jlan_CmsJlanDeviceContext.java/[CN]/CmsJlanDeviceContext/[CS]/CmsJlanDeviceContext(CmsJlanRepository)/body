{
  super(repo.getName());
  m_repository=repo;
  setFilesystemAttributes(FileSystem.CasePreservedNames | FileSystem.UnicodeOnDisk | FileSystem.CaseSensitiveSearch);
  SrvDiskInfo diskInfo=new SrvDiskInfo(2560000,64,512,2304000);
  setDiskInformation(diskInfo);
}
