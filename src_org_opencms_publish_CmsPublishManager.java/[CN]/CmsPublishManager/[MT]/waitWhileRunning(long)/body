{
  int i=0;
synchronized (this) {
    while (isRunning() && ((MS_ONE_SECOND * i) <= ms)) {
      try {
        this.wait(MS_ONE_SECOND);
      }
 catch (      InterruptedException e) {
        e.printStackTrace();
      }
      i++;
    }
  }
}
