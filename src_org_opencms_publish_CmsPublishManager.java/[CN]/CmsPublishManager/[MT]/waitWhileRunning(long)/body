{
  int i=0;
synchronized (this) {
    try {
      while (isRunning() && ((MS_ONE_SECOND * i) <= ms)) {
        this.wait(MS_ONE_SECOND);
        i++;
      }
    }
 catch (    InterruptedException e) {
    }
  }
}
