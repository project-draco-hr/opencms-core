{
  CmsSessionQueue queue=new CmsSessionQueue(config.needsQueue(),config.getQueueInterval().isPresent() ? config.getQueueInterval().get().longValue() : 0,config.getMaxQueueLength().isPresent() ? config.getMaxQueueLength().get().intValue() : 0);
  return queue;
}
