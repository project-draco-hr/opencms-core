{
  long date1=o1.m_dateChanged;
  long date2=o2.m_dateChanged;
  if (date1 < date2) {
    return -1;
  }
 else   if (date2 < date1) {
    return +1;
  }
 else {
    return o1.m_name.compareTo(o2.m_name);
  }
}
