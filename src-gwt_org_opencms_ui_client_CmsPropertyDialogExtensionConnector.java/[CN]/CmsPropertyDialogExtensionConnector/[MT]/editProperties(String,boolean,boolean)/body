{
  CmsEditProperties.PropertyEditingContext context=new CmsEditProperties.PropertyEditingContext();
  if (!disablePrevNext) {
    context.setMultiFileNavigation(this);
  }
  CmsEditProperties.editPropertiesWithFileNavigation(new CmsUUID(editStructureId),new ContextMenuHandler(),editName,new Runnable(){
    public void run(){
    }
  }
,false,context,null);
}
