{
  int portnumber=CmsXmlTemplateLoader.getRequest(cms.getRequestContext()).getServerPort();
  int limitedPort=cms.getLimitedWorkplacePort();
  return ((limitedPort == -1) || (limitedPort == portnumber));
}
