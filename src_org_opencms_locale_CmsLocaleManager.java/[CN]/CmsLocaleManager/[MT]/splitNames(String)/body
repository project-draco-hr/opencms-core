{
  if (names == null) {
    return null;
  }
  String result[]=CmsStringSubstitution.split(names,",");
  for (int i=0; i < result.length; i++) {
    result[i]=result[i].trim();
  }
  return result;
}
