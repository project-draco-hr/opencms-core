{
  checkIndexBoundsForInsertion(beforeIndex);
  if (child.getParent() == this) {
    int idx=getWidgetIndex(child);
    if (idx < beforeIndex) {
      beforeIndex--;
    }
  }
  return beforeIndex;
}
