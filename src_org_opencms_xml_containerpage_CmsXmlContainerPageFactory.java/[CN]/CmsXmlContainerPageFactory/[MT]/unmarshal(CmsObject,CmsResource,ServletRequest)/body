{
  String rootPath=resource.getRootPath();
  if (!CmsResourceTypeXmlContent.isXmlContent(resource) && !CmsResourceTypeContainerPage.isContainerPage(resource)) {
    throw new CmsXmlException(Messages.get().container(Messages.ERR_XMLCONTENT_INVALID_TYPE_1,cms.getSitePath(resource)));
  }
  CmsXmlContainerPage content=(CmsXmlContainerPage)req.getAttribute(rootPath);
  if (content == null) {
    content=unmarshal(cms,cms.readFile(resource));
    req.setAttribute(rootPath,content);
  }
  return content;
}
