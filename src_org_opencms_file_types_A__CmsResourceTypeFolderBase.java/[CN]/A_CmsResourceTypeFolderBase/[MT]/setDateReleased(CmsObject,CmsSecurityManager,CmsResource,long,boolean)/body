{
  super.setDateReleased(cms,securityManager,resource,dateLastModified,recursive);
  if (recursive) {
    List resources=securityManager.readChildResources(cms.getRequestContext(),resource,CmsResourceFilter.IGNORE_EXPIRATION,true,true);
    for (int i=0; i < resources.size(); i++) {
      CmsResource childResource=(CmsResource)resources.get(i);
      if (childResource.isFolder()) {
        setDateReleased(cms,securityManager,childResource,dateLastModified,recursive);
      }
 else {
        getResourceType(childResource.getTypeId()).setDateReleased(cms,securityManager,childResource,dateLastModified,recursive);
      }
    }
  }
}
