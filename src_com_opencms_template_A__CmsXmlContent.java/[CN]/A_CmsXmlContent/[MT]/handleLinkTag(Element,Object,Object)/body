{
  Element dBlock=(Element)n.cloneNode(true);
  processNode(dBlock,m_mainProcessTags,null,callingObject,userObj,null);
  String link=getTagValue(dBlock);
  return A_OpenCms.getLinkManager().substituteLink(m_cms,link);
}
