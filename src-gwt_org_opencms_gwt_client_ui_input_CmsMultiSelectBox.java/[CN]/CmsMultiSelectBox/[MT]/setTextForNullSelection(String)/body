{
  CmsMultiSelectCell cell=m_selectCells.get("");
  if (cell == null) {
    return;
  }
  cell.setTitle(text);
  if (CmsStringUtil.isEmptyOrWhitespaceOnly(m_selectedValue)) {
    selectValue("");
  }
}
