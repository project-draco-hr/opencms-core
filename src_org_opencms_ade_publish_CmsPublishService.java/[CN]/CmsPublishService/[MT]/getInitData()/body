{
  CmsPublishData result=null;
  try {
    CmsPublishOptions options=getCachedOptions();
    result=new CmsPublishData(options,getProjects(),getPublishGroups(options),OpenCms.getWorkflowManager().getAvailableActions(getCmsObject()),canPublishBrokenRelations(getCmsObject()));
  }
 catch (  Throwable e) {
    error(e);
  }
  return result;
}
