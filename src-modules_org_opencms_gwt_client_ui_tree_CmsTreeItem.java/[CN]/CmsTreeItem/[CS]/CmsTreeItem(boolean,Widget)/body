{
  this();
  if (content.length > 0) {
    for (int i=0; i < content.length - 1; i++) {
      m_content.addToFloat(content[i]);
    }
    add(content[content.length - 1]);
  }
  if (!showOpeners) {
    hideOpeners();
  }
}
