{
  if ((index <= 0) || (parent == null)) {
    return false;
  }
  if (draggable instanceof CmsTreeItem) {
    int originalPathLevel=getPathLevel(((CmsTreeItem)draggable).getPath());
    return originalPathLevel - 1 != getPathLevel(parent.getPath());
  }
  return true;
}
