{
  if (m_xpaths == null) {
    m_xpaths=new ArrayList<String>();
    m_xpaths.add("/" + CmsConfigurationManager.N_ROOT + "/"+ CmsSystemConfiguration.N_SYSTEM+ "/"+ CmsSystemConfiguration.N_SUBSCRIPTIONMANAGER);
  }
  return m_xpaths;
}
