{
  m_loginMessage=message;
  if (CmsLog.INIT.isInfoEnabled()) {
    CmsLog.INIT.info(Messages.get().key(Messages.INIT_LOGINMESSAGE_3,new Boolean(message.isEnabled()),new Boolean(message.isLoginForbidden()),message.getMessage()));
  }
}
