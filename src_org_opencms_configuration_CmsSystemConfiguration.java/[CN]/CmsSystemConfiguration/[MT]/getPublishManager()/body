{
  if (m_publishManager == null) {
    m_publishManager=new CmsPublishManager(CmsPublishManager.DEFAULT_HISTORY_SIZE,CmsPublishManager.DEFAULT_QUEUE_PERSISTANCE,CmsPublishManager.DEFAULT_QUEUE_SHUTDOWNTIME);
  }
  return m_publishManager;
}
