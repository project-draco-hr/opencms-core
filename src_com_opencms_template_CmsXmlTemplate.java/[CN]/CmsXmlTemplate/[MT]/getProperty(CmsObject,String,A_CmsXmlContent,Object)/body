{
  String requestedUri=cms.getRequestContext().getUri();
  String value="";
  try {
    value=cms.readProperty(requestedUri,tagcontent);
  }
 catch (  Exception e) {
    if (OpenCms.isLogging(CmsLog.C_OPENCMS_INFO,CmsLog.LEVEL_WARN)) {
      OpenCms.log(CmsLog.C_OPENCMS_INFO,CmsLog.LEVEL_WARN,"[CmsXmlTemplate] usermethod getProperty throwed an Exception getting " + tagcontent + ": "+ e.toString());
    }
  }
  if (value == null) {
    value="";
  }
  return value;
}
