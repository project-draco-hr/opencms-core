{
  if (template != null) {
    CmsSitemapTemplate result=new CmsSitemapTemplate(template.getTitle(),template.getDescription(),"",template.getImgPath());
    result.setShowWeakText(true);
    return result;
  }
 else {
    return CmsSitemapTemplate.getNullTemplate();
  }
}
