{
  CmsSite site=new CmsSite();
  site.setName(siteName);
  site.setDescription("MultisiteTest");
  site.setOnlineProjectId(C_PROJECT_ONLINE_ID);
  site.setGuestGroup(C_GROUP_GUEST);
  site.setGuestUser(C_USER_GUEST);
  return site;
}
