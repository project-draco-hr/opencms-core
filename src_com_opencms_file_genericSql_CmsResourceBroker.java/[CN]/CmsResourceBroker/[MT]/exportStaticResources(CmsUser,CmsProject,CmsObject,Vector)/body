{
  if (isAdmin(currentUser,currentProject)) {
    new CmsStaticExport(cms,startpoints);
  }
 else {
    throw new CmsException("[" + this.getClass().getName() + "] exportResources",CmsException.C_NO_ACCESS);
  }
}
