{
  int project=resource.getProjectId();
  if (!accessProject(currentUser,currentProject,project)) {
    return (false);
  }
  CmsResource res=resource;
  for (; ; ) {
    if (!accessOther(currentUser,currentProject,resource,C_ACCESS_PUBLIC_READ) && !accessOwner(currentUser,currentProject,resource,C_ACCESS_OWNER_READ) && !accessGroup(currentUser,currentProject,resource,C_ACCESS_GROUP_READ))     return false;
    if (res.getParent() != null)     res=readFolder(currentUser,currentProject,project,res.getParent());
 else     return true;
    if (res == null)     throw new CmsException(this.getClass().getName() + ".accessRead(): Cannot find \'" + res.getName()+ "\'  while examening \'"+ resource.getName()+ "\'",CmsException.C_NOT_FOUND);
  }
}
