{
  CmsUser user=(CmsUser)m_userCache.get(new CacheId(userName + userType));
  if (user == null) {
    user=m_dbAccess.readUser(userName,userType);
    m_userCache.put(new CacheId(user),user);
  }
  return user;
}
