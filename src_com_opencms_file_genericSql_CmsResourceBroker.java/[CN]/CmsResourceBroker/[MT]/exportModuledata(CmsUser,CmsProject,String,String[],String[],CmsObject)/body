{
  if (isAdmin(currentUser,currentProject)) {
    new CmsExportModuledata(exportFile,exportChannels,exportModules,cms);
  }
 else {
    throw new CmsException("[" + this.getClass().getName() + "] exportModuledata",CmsException.C_NO_ACCESS);
  }
}
