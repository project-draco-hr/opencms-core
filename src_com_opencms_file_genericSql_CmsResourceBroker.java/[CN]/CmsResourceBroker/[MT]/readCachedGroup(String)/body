{
  CmsGroup group=(CmsGroup)m_groupCache.get(new CacheId(groupName));
  if (group == null) {
    group=m_dbAccess.readGroup(groupName);
    m_groupCache.put(new CacheId(group),group);
  }
  return group;
}
