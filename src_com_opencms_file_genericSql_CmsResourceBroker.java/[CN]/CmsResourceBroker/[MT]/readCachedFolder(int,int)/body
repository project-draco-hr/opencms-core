{
  String cacheKey=getCacheKey("folder_id_cache",projectId,"" + folderId);
  CmsFolder folder=(CmsFolder)m_resourceCache.get(cacheKey);
  if (folder == null) {
    folder=m_dbAccess.readFolder(projectId,folderId);
    m_resourceCache.put(cacheKey,folder);
  }
  return folder;
}
