{
  CmsClientSitemapEntry entry=getHoverbar().getEntry();
  CmsUUID id=entry.getId();
  editModelPage(entry.getSitePath(),!CmsSitemapView.getInstance().isModelPageEntry(id) && !CmsSitemapView.getInstance().isParentModelPageEntry(id));
}
