{
  this(entry.getId(),entry.getOriginalUri(),entry.getStructureId(),entry.getName(),entry.getTitle(),entry.isRootEntry(),entry.getNewProperties(),entry.getSubEntries(),entry.getContentId(),entry.getContentName());
  setRuntimeInfo("",entry.getPosition(),entry.getComputedProperties());
}
