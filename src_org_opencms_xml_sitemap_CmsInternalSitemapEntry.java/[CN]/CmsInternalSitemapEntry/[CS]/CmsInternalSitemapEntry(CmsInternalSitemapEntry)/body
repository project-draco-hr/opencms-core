{
  this(entry.getId(),entry.getOriginalUri(),entry.getStructureId(),entry.getName(),entry.getTitle(),entry.isRootEntry(),entry.getNewProperties(),entry.getSubEntries(),entry.getContentId());
  setRuntimeInfo("",entry.getPosition(),entry.getComputedProperties());
}
