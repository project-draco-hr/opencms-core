{
  String name=null;
  if (!m_resource.endsWith("/")) {
    name=m_resource.substring(m_resource.lastIndexOf("/") + 1,m_resource.length());
  }
 else {
    name=m_resource.substring(m_resource.substring(0,m_resource.length() - 1).lastIndexOf("/") + 1,m_resource.length());
  }
  return name;
}
