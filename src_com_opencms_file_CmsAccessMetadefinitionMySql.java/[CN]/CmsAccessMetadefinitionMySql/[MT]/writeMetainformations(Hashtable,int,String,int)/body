{
  Enumeration keys=metainfos.keys();
  String key;
  while (keys.hasMoreElements()) {
    key=(String)keys.nextElement();
    writeMetainformation(key,(String)metainfos.get(key),projectId,path,resourceType);
  }
}
