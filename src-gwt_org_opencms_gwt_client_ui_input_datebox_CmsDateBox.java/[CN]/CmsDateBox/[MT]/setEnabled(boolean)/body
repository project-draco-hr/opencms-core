{
  if (!enabled) {
    m_tmpValue=getValue();
    m_box.setFormValueAsString(Messages.get().key(Messages.GUI_INPUT_NOT_USED_0));
  }
 else {
    setValue(m_tmpValue);
  }
  m_box.setEnabled(enabled);
}
