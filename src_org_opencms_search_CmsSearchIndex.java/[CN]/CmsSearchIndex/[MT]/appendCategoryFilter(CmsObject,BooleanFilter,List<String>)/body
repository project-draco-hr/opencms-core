{
  if ((categories != null) && (categories.size() > 0)) {
    filter.add(new FilterClause(getMultiTermQueryFilter(CmsSearchField.FIELD_CATEGORY,categories),BooleanClause.Occur.MUST));
  }
  return filter;
}
