{
  if (m_searcher != null) {
    try {
      m_searcher.close();
    }
 catch (    Exception e) {
      LOG.error(Messages.get().getBundle().key(Messages.ERR_INDEX_SHUTDOWN_1,getName()),e);
    }
  }
}
