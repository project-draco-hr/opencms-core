{
  if (!ignoreSearchExclude) {
    filter.add(new FilterClause(getMultiTermQueryFilter(CmsSearchField.FIELD_SEARCH_EXCLUDE,EXCLUDE_PROPERTY_VALUES),BooleanClause.Occur.MUST_NOT));
  }
  return filter;
}
