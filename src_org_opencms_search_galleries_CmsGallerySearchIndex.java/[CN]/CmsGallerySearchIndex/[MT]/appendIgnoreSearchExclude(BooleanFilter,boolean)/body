{
  if (!ignoreSearchExclude) {
    filter.add(new FilterClause(getMultiTermQueryFilter(I_CmsSearchField.FIELD_SEARCH_EXCLUDE,EXCLUDE_PROPERTY_VALUES),BooleanClause.Occur.MUST_NOT));
  }
  return filter;
}
