{
  if (obj == this) {
    return true;
  }
  if (obj instanceof CmsLock) {
    CmsLock otherLock=(CmsLock)obj;
    return otherLock.m_resourceName.equals(m_resourceName) && otherLock.m_userId.equals(m_userId) && otherLock.m_projectId == m_projectId;
  }
  return false;
}
