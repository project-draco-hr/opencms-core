{
  if (m_lock == null) {
    CmsLock lock;
    try {
      lock=getCms().getLock(m_resource);
    }
 catch (    CmsException e) {
      lock=CmsLock.getNullLock();
    }
    m_lock=lock;
  }
  return m_lock;
}
