{
  int projectId=m_resource.getProjectLastModified();
  if (!getLock().isNullLock() && getLock().getType() != CmsLock.TYPE_INHERITED && getLock().getType() != CmsLock.TYPE_SHARED_INHERITED) {
    projectId=getLock().getProjectId();
  }
  return projectId;
}
