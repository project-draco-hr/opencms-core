{
  String lockedBy="";
  if (!getLock().isNullLock()) {
    try {
      lockedBy=getCms().readUser(getLock().getUserId()).getName();
    }
 catch (    Throwable e) {
      lockedBy=e.getMessage();
    }
  }
  return lockedBy;
}
