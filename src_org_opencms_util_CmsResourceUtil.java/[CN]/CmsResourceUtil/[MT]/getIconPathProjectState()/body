{
  String iconPath;
  if (getProjectState() == null) {
    iconPath="none";
  }
 else   if (getProjectState().booleanValue()) {
    iconPath="this";
  }
 else {
    iconPath="other";
  }
  return "explorer/project_" + iconPath + ".gif";
}
