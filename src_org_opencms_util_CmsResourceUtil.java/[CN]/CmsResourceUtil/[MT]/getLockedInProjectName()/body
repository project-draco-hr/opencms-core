{
  try {
    int pId=getLockedInProjectId();
    if (pId == CmsDbUtil.UNKNOWN_ID) {
      return "";
    }
    return getCms().readProject(pId).getName();
  }
 catch (  Throwable e) {
    return e.getMessage();
  }
}
