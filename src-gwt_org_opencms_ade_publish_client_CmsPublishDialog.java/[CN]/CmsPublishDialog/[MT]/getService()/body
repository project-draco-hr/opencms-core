{
  if (SERVICE == null) {
    SERVICE=GWT.create(I_CmsPublishService.class);
    String serviceUrl=CmsStringUtil.joinPaths(CmsCoreProvider.get().getContext(),"org.opencms.ade.publish.CmsPublishService.gwt");
    ((ServiceDefTarget)SERVICE).setServiceEntryPoint(serviceUrl);
  }
  return SERVICE;
}
