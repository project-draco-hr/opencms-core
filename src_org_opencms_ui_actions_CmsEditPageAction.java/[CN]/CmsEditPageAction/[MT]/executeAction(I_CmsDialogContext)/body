{
  if ((context.getResources().size() == 1) && CmsResourceTypeXmlContainerPage.isContainerPage(context.getResources().get(0))) {
    A_CmsUI.get().getPage().setLocation(OpenCms.getLinkManager().substituteLink(A_CmsUI.getCmsObject(),context.getResources().get(0)));
  }
}
