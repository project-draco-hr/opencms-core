{
  I_CmsResourceType result=null;
  if (typeId < m_configuration.m_resourceTypes.length) {
    result=m_configuration.m_resourceTypes[typeId];
  }
  if (result == null) {
    throw new CmsLoaderException("Unknown resource type id requested: " + typeId,CmsLoaderException.C_LOADER_UNKNOWN_RESOURCE_TYPE);
  }
  return result;
}
