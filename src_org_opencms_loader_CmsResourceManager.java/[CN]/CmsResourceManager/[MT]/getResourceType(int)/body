{
  try {
    return m_resourceTypes[typeId];
  }
 catch (  Exception e) {
    throw new CmsException("[" + this.getClass().getName() + "] Unknown resource type id requested: "+ typeId,CmsException.C_NOT_FOUND);
  }
}
