{
  for (int i=0; i < m_resourceTypeList.size(); i++) {
    I_CmsResourceType type=(I_CmsResourceType)m_resourceTypeList.get(i);
    if (type.getTypeName().equals(typeName)) {
      return type;
    }
  }
  throw new CmsLoaderException("Unknown resource type name requested: " + typeName,CmsLoaderException.C_LOADER_UNKNOWN_RESOURCE_TYPE);
}
